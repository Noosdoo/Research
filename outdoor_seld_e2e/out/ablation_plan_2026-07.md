# 物理要素 Ablation 実験計画（ドラフト・ゼミ合意待ち）

作成: 2026-07-12。README.md「次の一手」item 1-2、PROGRESS.md v3節の「次」を具体化したもの。
サーベイ（`out/survey_novelty_2026-07.md`）の結論「新規性は "どの物理が効くか" の系統的定量化」
を実験として実行可能な形に落とす。**このドラフトをゼミで見せて実験範囲の合意を取るのが目的。**

## 1. これまでの結果から言えること（動機）

| 版 | 難易度軸 | 最終指標（val） | 要点 |
|---|---|---|---|
| v1 | なし（クリーン・常時発音） | ER 0.000 / LE 1.8° / SELD 0.003 | 天井効果。物理は正しいが差が出ない |
| v2 | 背景雑音 SNR 0-20dB | ER 0.000 / LE 3.4° / SELD 0.005 | 定常拡散雑音だけでは崩れない |
| v3 | ＋スパース発音＋車妨害音 SIR 0-15dB | ER 0.042 / LE 6.5° / SELD 0.027 | 初めて誤りが残存。誤り解剖済み |

v3 の誤り解剖（`out/figures_v3_analysis/summary.json`、20クリップ・738正解フレーム）:
- miss 6 / fa 10 / dir_err(>20°) 21 / ok 711
- **miss は6件中6件すべて発音区間の端（±0.3秒）で発生**（オンセット/オフセットの曖昧さ。
  中間フレームでの見逃しはゼロ）
- fa は発音区間内外でほぼ半々（6 vs 4）。車方位との整合は fa10件中5件のみ20°以内
  （中央ずれ57.8°）→ 誤通知は「車を誤認識」だけでは説明できず、他要因（残響・雑音）が疑われる
- dir_err（方向外れ）が最大のカテゴリ。SIR/SNRとの相関は `error_anatomy.png` 参照

→ **「難易度は上げられたが、どの物理要因がどの誤りタイプを生むかはまだ切り分けられていない。**
これが ablation の存在理由。単一のSNR/SIR軸だけでは miss/fa/dir_err を区別する説明変数が
不足している（例: dir_errは大気吸収/地形反射由来か、単に妨害音由来か不明）。

（注: v3は2026-07-12にcar+drone混在バリアントの実験→不採用判断→復元、という経緯を経ている。
復元後の再学習でER 0.042/F96.9%/LE6.5°/SELD_scr0.027を再現し、上表はこの値に更新済み。
detailはPROGRESS.md「v3 ドローン混入版の破棄と復元」節参照）

## 1.5 土俵依存性についての注意（2026-07-12追記）

この ablation は **v3という1つの土俵（住宅街・速度5-15m/s・距離3-15m）の上でしか実施しない**。
土俵のパラメータ（速度・距離・SNR/SIRレンジ）は、それぞれ異なる物理要素の「見えやすさ」を
決めてしまう（例: 距離レンジが近ければ大気吸収は効きにくく見える、速度が遅ければドップラーの
寄与は小さく見える）。したがって本実験の結論は「屋外SELD一般において」ではなく
**「住宅街・低〜中速の通過シーンにおいて」**とスコープを明示する。v3の速度・距離レンジ自体は
恣意的ではなく、6月の実録音分析（住宅街では距離・SNRが効く）に基づく。
一般化の検証は4節の拡張候補（v4）を参照。

## 2. 対象とする物理要素（4軸、on/off ないし段階）

`src/outdoor_seld/fastsim.py` の `render_mono()` が全物理を1関数に集約しているため、
各要素は関数内の一箇所を無効化するだけで切り替えられる（DynamicSound側の変更は不要）。

| # | 要素 | 実装箇所 | off時の代替 | 仮説 |
|---|---|---|---|---|
| A | ドップラー | 放射時刻ソルバ `solve_emission_times`（①） | 音源位置を全フレーム固定（現在時刻の幾何のみで読み出し） | LE・dir_errへの寄与は小さい（v1-v3how でLEは既にSIR/SNR主導）。むしろ**速度が速い条件でのみ効く**という交互作用が主仮説 |
| B | 大気吸収 | ISO9613-1 FIR（④） | FIRをスキップ（減衰なし） | 高周波減衰が消える→遠距離クリップでFA/dir_err微増 |
| C | 幾何減衰 1/r | `g[ok] = 1/dist[ok]`（③） | g=1（距離非依存） | 遠距離クリップのSNR実効値が変わる→miss増（特に|y|大きい条件） |
| D | 地面反射 | step6の `foa_direct + foa_mirror` 加算 | mirrorを加算しない（直接音のみ） | 反射で生じる疑似的な追加到来方向がdir_err/faの一因という仮説 |

各要素は独立にon/off可能（2^4=16条件）だが、全数は過剰。3節で絞る。

## 3. 実験計画（絞り込み案）

### 3.1 データセットは作り直さない — 同一の40/80シーンで音声だけ再レンダリング

ジオメトリ・雑音・妨害音の乱数条件（`scene.json` に既存）は固定し、`fastsim.render_mono`
の物理フラグだけ変えて `foa/*.flac` を差し替える。**ラベルは変わらない**（ラベルは幾何のみで
決まり、放射時刻はA=onの場合と同一の式を使うため）。→ 1条件 = 全80本で約3分（実測済みの
330倍速レンダラのため）。

### 3.2 提案する条件セット（5条件、フルグリッドではなく one-factor-at-a-time）

| 条件 | A doppler | B 大気吸収 | C 1/r | D 地面反射 | 目的 |
|---|---|---|---|---|---|
| full（v3そのまま） | on | on | on | on | 基準点（既存結果を流用、再学習不要） |
| no-doppler | off | on | on | on | ドップラー単独の寄与 |
| no-airabs | on | off | on | on | 大気吸収単独の寄与 |
| no-1/r | on | on | off | on | 距離減衰単独の寄与 |
| no-ground | on | on | on | off | 地面反射単独の寄与 |

- one-factor-at-a-time（要素数と同数=4本の再学習）を第一候補とする。交互作用
  （例: 「速度が速い条件でのみドップラー除去が効く」= A×速度）は per-clip の速度/距離
  メタデータ（`scene.json`）を使って**単一モデルの結果を層別分析**すれば見える可能性が高く、
  総当たり16条件を学習する前にまず one-factor-at-a-time＋層別分析で見えるかを確認する
  （時間対効果。フルグリッドは差が見えた要素についてのみ後追いで拡張）。
- 学習は毎回 v1 と同じ基盤ckptから（PROGRESS.md既定方針を踏襲、実験間の初期条件を統一）。
- 各条件 70ep・T4・batch_size=8（v1-v3と同一設定）。Colab上で1本 約15分 → 4本で約1時間。

### 3.3 評価: 誤り解剖を条件間で横並びにする

`scripts/step7_error_anatomy.py` を条件ごとに実行し、以下を主表にする（ゼミの主図候補）:

| 条件 | ER | LE | SELD_scr | miss | fa | dir_err | miss@edge比率 | fa@car比率 |
|---|---|---|---|---|---|---|---|---|
| full | 0.042 | 7.3° | 0.030 | 6 | 10 | 21 | 100% | 50% |
| no-doppler | ? | ? | ? | ? | ? | ? | ? | ? |
| no-airabs | … | | | | | | | |
| no-1/r | … | | | | | | | |
| no-ground | … | | | | | | | |

miss@edge比率・fa@car比率が条件間で動くかどうかが「どの物理がどの誤りタイプを作るか」の
直接証拠になる。動かなければ「これらの物理要素はこの難易度帯では効かない」という結果自体が
サーベイの主張（系統的定量化）に使える。

## 4. スコープ外・保留

- フルグリッド16条件: one-factor-at-a-timeで有意な要素が見つかった場合のみ、その要素×他1-2要素
  の交互作用を追加検証（時間予算次第）
- 複数シード（PROGRESS.md課題(b)）: 今回は1シードで実施し、差が僅かな場合のみ複数シード化を検討
- SIR負側拡張v3.1（課題(c)）: ablationの結果を見てから判断（誤りが小さすぎる場合の追加難易度化）
- 回折・複数反射面（ストリートキャニオン等）: 現物理エンジン（DynamicSound）の範囲外。
  対応しない方針でユーザーと合意済み（2026-07-12）

## 4.5 拡張候補: v4 幹線道路版（土俵依存性の検証、優先度最上位）

1.5節の「土俵依存性」への対処として、**速度・距離レンジだけを変えた第二の土俵（v4）**を
用意し、同じablationを繰り返すことを検討中。既存物理エンジン・既存合成音源（siren.py）
のままレンジ設定を変えるだけなので新規実装は不要。v3と結果を比較し、物理要素の効き方が
土俵で変わるかどうか自体を追加の観察対象にする。

**運用ルール（2026-07-12合意）**: v4は既存のv1/v2/v3を一切上書きせず、新規フォルダ
（`out/dataset_outdoor_siren_v4/`）・新規zip・必要なら新規Colabノートブックとして追加する。
新しい音源要素（クラクション等、優先度は下）を足す場合もまずクリーン合成音源から試す
（実録音の混在は土俵の解釈可能性を崩すリスクがあるため。car+dronec混在版が破棄された理由）。

## 5. ゼミで合意を取りたい点

1. one-factor-at-a-time（4本）を先にやる、というスコープ判断でよいか
2. 主表の形（上記3.3）で「差がない」という結果が出た場合も卒論の主張として成立するか
   （サーベイの言い回し上は「効かないことの定量化」も許容範囲、要指導教員確認）
3. 複数シードをやらない前提（時間予算）でよいか
4. 結論のスコープを「住宅街・低〜中速」に限定する言い回しでよいか、v4（幹線道路版）による
   土俵依存性の検証まで卒論に含めるべきか（1.5節・4.5節参照）

---

## 6. 【2026-07-14 更新】v5〜v7の結果を受けた改訂案（こちらを最新とする）

上の1〜5節はv3時代のドラフト（記録として保存）。以降の進展で前提が変わったため、
実行時はこの6節の形を提案する。**実行はゼミ合意後**（本人指示 2026-07-14 再確認済み）。

### 6.1 基準土俵の変更: v3 → v7（フル物理・4クラス・320本）

- v7 = v6と同一の320シーン（4クラス各80本、クリーン合成妨害車）に**地面反射を加えた
  フル物理版**（two-ray像音源・剛体地面R=+1、対象音と妨害車の両方）。詳細はPROGRESS.md
- 旧計画のD（地面反射）は「基準に無いものを足す」非対称な条件だったが、v7を基準に
  すれば**全条件が「フル物理から1要素外す」対称なone-factor-at-a-time**になる
- 4クラス化の利点: 物理要素×音クラスの応答が見える（大気吸収は3kHzのBikeBellに、
  ドップラー検出は純音のBackupBeepに最も効くはず等）。クラス別誤り解剖
  （step8_error_anatomy_mc.py、v5/v6で検証済み）が計器になる

### 6.2 実装状況（準備は完了、未実行）

- fastsim `render_mono` に enable_doppler / enable_spreading / enable_air_absorption
  実装済み（既定全ON=git HEADとビット単位一致を確認、off時の物理検証も全PASS）
- **doppler off = 一定遅延読み出し** と定義: 静的RIRの補間で移動音源を作る既存SELD生成器
  （SpatialScaper, arXiv:2401.12238）が暗黙に行う近似と同じ → 「no-doppler条件 =
  既存生成器の世界の再現」という位置づけができ、ablationがそのまま
  「既存生成器に何が足りないか」の答えになる
- 地面反射offは鏡像レンダの加算有無（step6レベル）で従来から切替可能

### 6.3 条件セット改訂（学習は実質3本で済む）

| 条件 | 内容 | データ生成 | 学習 |
|---|---|---|---|
| full | v7そのまま | 済（2026-07-14） | v7 run1（実行待ち） |
| no-ground | v7から反射を外す = **v6と同一データ** | **不要（v6を流用）** | **不要（v6 run1を流用）** |
| no-doppler | v7の全音源を enable_doppler=False で再レンダ | 約40分 | 1本 |
| no-airabs | 同 enable_air_absorption=False | 約40分 | 1本 |
| no-1/r | 同 enable_spreading=False | 約40分 | 1本 |

- v6がno-ground条件そのものになっている（v7との差は反射のみ）ため、追加学習は3本。
  Colab 100ep×約25分 → 全部で約1.5時間＋zipアップロード3回（各637MB）

### 6.4 評価設計（走らせる前に固定する）

- **主比較: 各条件のモデルを「フル物理(v7)の共通val 80本」で評価**（train=欠けた世界、
  eval=完全な世界 = sim-to-realギャップの縮図）。v5〜v7はシーン分割が同一
  （train=idx0-239 / val=idx240-319）なのでクロス評価にリークなし
- **主指標はクラス別の解剖指標**（dir_err率・LE中央値・fa・miss@edge）。全体SELD_scrは
  天井（v6で0.028）に近く感度不足のため参考値扱い。v5→v6比較でこの計器が
  27.8%→0.6%の差を検出できることは実証済み
- **ノイズフロア**: full条件（v7）をシード違いでもう1本学習し、run間のブレ幅を先に測る。
  条件間の差は「ブレ幅を超えた分」だけを解釈する（旧計画の「複数シードなし」の最小修正、
  追加コストはColab約25分×1）

### 6.5 ゼミで合意を取りたい点（改訂）

1. 基準土俵をv3からv7（フル物理・4クラス）に変えてよいか
2. no-ground条件としてv6（データ・学習とも既存）を流用してよいか
3. ノイズフロア用のシード違い1本を追加する（複数シードの最小版）でよいか
4. 「差がない」結果も主張として成立するか（旧5節の2と同じ、要指導教員確認）
5. v3/v4（シングルクラス2土俵）でのablationは、v7で差が見えた要素のみ後追いするで
   よいか（土俵依存性の確認、旧4.5節の縮小版）
