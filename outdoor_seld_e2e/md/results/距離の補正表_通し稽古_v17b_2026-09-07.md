# 距離の補正表 — 手順の通し稽古（v17b・2026-09-07）

本人決定（2026-09-07 07:05）: v2 の束に「距離の補正表」（束 §6.1 判断 1・B 後から補正）を**作る**。数値は v2 モデルで作り直すので、
ここでは v17b e139 で**手順そのもの**を通し、何を見て固定するかを決める。手順 = `scripts/_dist_calib_proto.py`（0.25 m ビンの実距離中央値を単調化）
→ `--dist-table` で規則の前に当てる（`_long_score.py` / `_long_breakdown.py`）。

## 1. 先にわかったこと: 試作表（ft2 で作成）は長尺セットの鳴らしすぎを増やす

ft2 e079 の fold32 で作った試作表（`out/hp_sweep/dist_calib_proto.json`・1.5〜3 m の予測を 0.05〜0.10 m 縮める）を ft2 の長尺 fold40 に当てた:

| 予測（fold40・200 本） | 中/分 | 強/分 | 至近の強到達 | 幹線歩道: 安全な車への中 | 同・強 |
| --- | --- | --- | --- | --- | --- |
| ft2 e099 そのまま | 1.38 | 0.90 | 94.4% | 68%（170 件中） | 34%（76 件中） |
| ft2 ＋ 試作表 | **1.55** | 0.94 | 94.4% | **70%**（200 件中） | 36%（80 件中） |
| fold41: ft2 そのまま | 1.40 | 0.89 | 82.9% | 55%（201 件中） | 39%（96 件中） |
| fold41: ft2 ＋ 試作表 | **1.52** | 0.98 | 85.4% | **61%**（218 件中） | 38%（112 件中） |

表は「至近の車を至近と言う」ために距離を縮める向きなので、規則の閾値（中 1.6 m）を越える車が増え、**注意（中）が 12% 増える**。
至近捕捉（10 秒 val で 61.7→69.1%）と鳴らしすぎは同じつまみの表裏で、**片方だけ見て固定してはいけない**。

→ v2 の手順に追加する確認: 表を当てた後の長尺セット（fold40/41）で「中/分」「安全な車への中・強の率」が**悪化しない**ことを固定の条件に入れる（悪化するなら表の適用範囲を至近帯 ≤1.5 m の予測に限る、または表を使わない）。

同じ試作表を 10 秒の開発 val（v12 fold2・統一採点器 `--dist-table`）で見ると: ft2 e079 の 至近捕捉(全GT) 60.2→**66.2%**、強到達 81.5→83.4%、
**安全抑制 88.1→86.9%**、距離誤差 9.4→9.5%（`out/audit_rescore/table_check_ft2.md`）。10 秒 val でも「捕捉↑・抑制↓」の表裏が出る。

補足: 試作表を「予測 ≤1.5 m だけ」に当てる（`--dist-table-max 1.5`）と、fold40/41 とも**表なしと同一**の結果になった。試作表の ≤1.5 m のビンはほぼ恒等で、
効いていたのは 1.5〜3 m の縮めだけ。つまり ft2 の「至近を遠く言う」癖は表では戻せず（試作時の結論どおり）、表の効果は「1.5 m のしきい値をまたぐ取りこぼし」対策＝注意が増える副作用と表裏。

## 2. v17b での通し稽古（fold32 で作成 → 別データで確認 → 長尺で鳴らしすぎ）

v17b e139 の因果推論（ジョブ 4018）。GT は水平距離（v43tune/v42tune の 3D ラベルを `d×cos(仰角)` で水平化 = step19_dist_labels_v16 と同じ式）。
表 = `out/hp_sweep/dist_calib_v17b_e139.json`（fold32・ペア 90,576）。

| 推定距離のビン | ペア数 | 補正量 |
| --- | --- | --- |
| 0.25–0.50 m | 36 | +0.16 m（ペア少・参考） |
| 0.50–1.00 m | 398 | +0.02〜+0.06 m |
| 1.00–1.25 m | 467 | −0.04 m |
| 1.25–3.00 m | 8,784 | **−0.08〜−0.11 m**（v17b はこの帯で 1 割ほど遠く言う） |

| 確認 | 至近捕捉 | 誤捕捉 | 至近推定中央値 | 距離誤差 | 帯別バイアス 1.0–1.5 / 1.5–2.5 / 2.5–4 m（前→後） |
| --- | --- | --- | --- | --- | --- |
| 作成データ fold32 | 58.1→**68.2%** | 0.02→0.03% | 1.42→1.32 m | 8.6→8.4% | +17→+9 / +9→+4 / +7→+1 |
| 別データ v12 fold2 val | 59.1→**68.7%** | 0.02→0.02% | 1.40→1.30 m | 9.1→8.9% | +17→+10 / +10→+5 / +7→+2 |
| fold30（確認用） | （ジョブ完了後に追記） | | | | |

統一採点器（v12 fold2 val・全 GT 分母・`table_check_v17b.md`）: 至近捕捉(全GT) 56.6→**65.9%**、強到達 82.3→83.6%、**安全抑制 92.0→90.0%**、距離誤差 8.8→8.6%。
表を予測 ≤1.5 m だけに限る（`--dist-table-max 1.5`）と、10 秒 val も長尺も**表なしと同一**（表の ≤1.5 m ビンはほぼ恒等・§1 の ft2 と同じ）。

長尺セット（`--dist-table`・幹線歩道の内訳は `breakdown_v17b_*_arterial_walk_table.md`）:

| セット | 予測 | 中/分 | 強/分 | 至近の強到達 | 幹線歩道: 安全な車への中 | 同・強 |
| --- | --- | --- | --- | --- | --- | --- |
| fold40 | v17b | 1.11 | 1.18 | 97.2% | 67%（150 件中） | 54%（104 件中） |
| fold40 | v17b ＋ 表 | **1.41**（+27%） | 1.23 | 97.2% | **71%**（197 件中） | 55%（108 件中） |
| fold41 | v17b | 1.24 | 1.21 | 92.7% | 61%（189 件中） | 46%（127 件中） |
| fold41 | v17b ＋ 表 | **1.47**（+19%） | 1.25 | 92.7% | **65%**（226 件中） | 48%（132 件中） |

**結論（v17b でも同じ）**: 表は 10 秒 val の至近捕捉を +10pt 上げるが、長尺の注意（中）を 2〜3 割増やし、安全な車への中・強を減らさない。
安全な車への「強」は表の前後でほぼ同じ（54→55%）なので、その原因は距離の平均的なずれではなく、規則の最接近予測（接近速度・方位の傾き）が
安全な車の経路を「近づく」と誤る側にある。表では直らない。

## 3. v2 の手順に入れる採用条件（案・事前登録の下書きに転記）

表を固定する条件を先に決めておく（数値は v2 モデルで測る）:

1. 10 秒 val（fold30・確認用）で至近捕捉(全GT) が **+5pt 以上**、誤捕捉が **+0.05pt 以内**
2. 長尺セット fold40/41 で **中/分の増加が +10% 以内**、幹線歩道の安全な車への強の率が **+2pt 以内**
3. 1 を満たし 2 を満たさない場合は**表なし**で確定評価に進む（「予測 ≤1.5 m だけに限る」案は、この種の表では表なしと同じ結果になるので選択肢から外す）
4. 表を使う・使わないの判断は確定評価の前に本票に書き、fold20 を見てから変えない

v17b で試すと 1 は満たす（+9.6pt）が 2 は満たさない（中 +19〜27%）ので、v17b のままなら「表なし」になる。
鳴らしすぎ（安全な車への強）は D6/D11（束の候補）か規則側の再検討が要り、v2 のデータ（D10 至近・低速の増強済み）でどう出るかを見てから決める。
