# ablation 確認run（2026-08-15、中間発表前・Opus 5 実施）

> **これは本番のablationではありません。** 本番は中間発表後にFableが、土俵を修正したうえで
> 事前登録（`アブレーション実験計画_2026-07.md` §9、必要なら§10）に沿って実施します。
> 本文書は**配管が通ることを確かめる回**の記録で、**ここで出る数値は卒論に使いません**。

## 0. なぜ分けるか（問いが違う）

| | 確認run（本書） | 本番（発表後・Fable） |
| --- | --- | --- |
| 問い | **パイプラインは通るか。地雷はどこか** | どの物理が学習に必要か |
| arm | no_1r 1本のみ | 4arm＋基準seed違い（ラン揺れフロア） |
| 土俵 | 現行v12のまま | ゼミ指摘を反映して修正する前提 |
| 結果の扱い | **卒論・発表に使わない**。申し送りだけ残す | 事前登録に沿って解釈・報告する |

数値を混ぜると「どちらの結果か」が後で分からなくなるため、出力先・実験名・文書を分離する。

## 1. 実施内容

- arm = **no_1r**（幾何減衰1/rを外す）。§9.5で最初に置いたarm＝差が最大の見込みで、
  計器が動くかの確認を兼ねられる
- 連鎖: 生成 → 距離ラベル＋前処理 → 学習 → **自条件val**（その世界で学習しその世界で採点）
  ＋**フル物理val**（基準の世界で採点＝転移ギャップ）の2本推論
- 学習条件は基準(job315 = v12 w3)と**完全同一**: 初期重み `v12_init_from_run3ep84.ckpt`、
  `SDE_W_DIST=3.0`、`SDE_DIST_MODE=log`、100 epoch。**変えるのはデータの物理だけ**

## 2. 基準を絶対に触らない設計（本人指示 2026-08-15）

1. **出力先の物理的分離**
   - データ: `out/dataset_outdoor_siren_v12_abl_<arm>/`（基準は `..._v12/`）
   - datasetsルート: `PSELDNets/datasets_v12_abl_<arm>/`（基準は `datasets_v12/`）
     - 中のシンボリックリンク名だけ `outdoor_siren_v12` にして、hydraの
       `dataset=outdoor_siren_v12` を変えずに `paths.dataset_dir` の差し替えだけで回す
     - こうしないと**前処理h5が基準ルートに書かれる**
   - 学習ログ: `experiment_name=outdoor_siren_v12_abl_<arm>`（基準は `..._sde_w3`）
2. **ドライバ側のガード**: `ABLATE` が空なら起動拒否、出力先が基準と同一なら assert 失敗
   （`scripts/_run_v12_abl_gen.py` / `_run_v12_abl_labels.py`）
3. **実行前に基準の指紋を記録**: `out/audit_logs/ablation_baseline_fingerprint_2026-08-15.txt`
   （各ディレクトリの files / bytes / (size,mtime,path)のsha256）。実行後に再検査して
   無変更を証明する
4. **ロールバック**: 基準に書き込まないので「戻す」操作は不要。arm側を消せば原状復帰
   （`rm -rf out/dataset_outdoor_siren_v12_abl_<arm> PSELDNets/datasets_v12_abl_<arm>`
   ＋ `PSELDNets_logs/.../runs/outdoor_siren_v12_abl_<arm>`）

## 3. 実測コスト（過去ジョブの実績から。2026-08-15確認）

| 工程 | 実績 | 出典 |
| --- | --- | --- |
| 生成（10,200本・12シャード並列） | **約70分**（4.1s/clip） | job248/249 = 1:09:52 / 1:12:02 |
| ラベル＋前処理 | **約2.5分** | job264 = 0:02:26 |
| 学習100ep＋val推論＋連結 | **約46分** | job315 = 0:45:50 |
| **arm 1本あたり合計** | **約2時間** | |

→ 本番の5本（4arm＋基準seed違い）は**実働8〜9時間**。1晩で終わる。

## 4. 見つかった地雷（**本番前に必読**）

### ① `_run_v12_gen.py` に ABLATE を付けて流してはいけない
出力先が `out/dataset_outdoor_siren_v12` 固定で、かつ既存foaをスキップする再開設計。
そのまま流すと **(a) 全部スキップして何も生成されない**（基準が埋まっている場合）か、
**(b) 基準をablation音声で上書きする**（空の場合）。
→ arm専用の `scripts/_run_v12_abl_gen.py` を新設して回避した。

### ② サーバで `git pull` してはいけない
リポジトリは `*.flac` のみ gitignore で、**metadata/masks のCSVは追跡対象**。
サーバ側でこれらは生成物として存在するため、`git pull` は
「ローカルの変更を上書きする」と判断して **abort する**（実際に2026-08-15にabortした）。
→ 新規ファイルは **scp で個別に転送**する。`git checkout -f` や `git pull -f` は
**絶対に使わない**（基準データセットのCSVが消える）。

### ③ 学習sbatchは `server_sde/` にある
`server/` には v11 系しか無い。v12の学習は `server_sde/v12_g45_w3.sbatch`（job315）。
sbatchは `~/research/PSELDNet/PSELDNets` へ cd してそこの `_train_sde12.py` を呼ぶので、
**PSELDNets側の同期が前提**（2026-08-15時点では同期済みを確認）。

### ④ 採点スクリプトも正解の参照先が基準固定（**黙って誤る**ので最も危険）
`_v12_score.py` は `DS12 = out/dataset_outdoor_siren_v12` を正解ラベルの参照先に固定している。
armの予測をそのまま流すと **armの予測を「基準の世界の正解」で採点**してしまう。
破壊はしないが**エラーも出ず誤った数値が出る**ため、地雷①②より発見が遅れる。
→ `scripts/_v12_abl_score.py` を新設し `--gt self|full` で明示切替する:
  - `--gt self` = armの世界の正解（自条件val）
  - `--gt full` = 基準の世界の正解（フル物理val＝**主要評価**）
また `_v12_score.py` / `_e0_probe_score.py` / `_score_sde_dist.py` は**サーバに存在しない**
（ローカルのみ）ので、採点前にscpが必要。

### ⑤ 学習の時間制限（**確認runで実際に切られた**）
a100の `3g.40gb` スライスでは **91.7秒/エポック → 100エポックに約2時間33分**かかる。
pro_6000の実績（46分）で `-t 02:00:00` を申告したところ、**エポック80で TIMEOUT**した。
→ 本番でa100_3gを使うなら **`-t 04:00:00` 以上**を申告する。pro_6000が空いているなら
そちらの方が3倍速い。なお `scontrol update TimeLimit=` による延長は**権限が無く不可**
（`Access/permission denied`）。チェックポイントは毎エポック `last.ckpt` に保存されるので、
切られても `server_sde/abl_infer.sbatch` で推論だけ回収できる。

### ⑥ 生成シャードの不均衡
12シャード均等割りだと最速2,388秒・最遅5,860秒（**2.5倍差**）で、遅いシャードに全体が
引きずられる。本番は**シャード数を増やす**か**行をシャッフルして割り当てる**と2〜3割縮む。

## 4-bis. 基準を触れないようにした仕組み（2026-08-15 本人指示「絶対に上書きするな」への対応）

1. **`server_sde/_baseline_guard.sh`** を新設し、`abl_labels.sbatch` と `abl_train.sbatch` の
   両方に組み込んだ。ジョブ冒頭で基準の指紋を記録し、末尾で照合して**1バイトでも違えば
   ジョブを失敗させる**。守る対象＝基準3データセット・基準datasetsルート・基準run・初期重みckpt
2. **`ABL_FULLVAL` フラグ**（既定0）を追加。学習ジョブで**唯一**基準ルートを参照する工程が
   「フル物理val推論」なので、確認runでは既定でスキップし自条件valのみ実行する。
   → 確認runは基準ルートに**読み書きとも一切アクセスしない**
   → **本番は `ABL_FULLVAL=1` が必須**（主要評価がこれ）
3. 書き込み先はこの3つのみ。ロールバックは削除するだけ:
   `out/dataset_outdoor_siren_v12_abl_<arm>/` /
   `PSELDNets/datasets_v12_abl_<arm>/` /
   `PSELDNets_logs/outdoor_siren_v12/runs/outdoor_siren_v12_abl_<arm>*`

## 5. 実行記録

| 項目 | 値 |
| --- | --- |
| 生成ジョブ | **job808**（a100_1g・MIGスライス。job807=pro_6000は22:58開始待ちのためcancel） |
| 投入コマンド | `sbatch -p a100_1g --gres=gpu:1g.10gb:1 -c 24 --mem=96G --export=ALL,ABLATE=no_1r server_sde/abl_gen.sbatch` |
| 投入時刻 | 2026-08-15 20:35（20:57時点 3,010/10,200・完了見込み21:51） |
| 基準の無変更 | 20:48に指紋再検査 → **5パスすべて実行前と完全一致** |
| ラベルジョブ | **job809** COMPLETED 0:02:43・ガードOK |
| 学習ジョブ | **job810** **TIMEOUT** 2:00:24 → **エポック80/100で打ち切り** |
| 推論ジョブ | **job811** COMPLETED 0:00:52（`last.ckpt`＝80エポック時点の重み） |
| 採点 | `out/abl_score_no_1r_selfval/`（自条件val。`_v12_abl_score.py --gt self`） |
| 基準の無変更 | **採点後も5パスすべて実行前と完全一致**（`out/audit_logs/ablation_after_fingerprint_2026-08-16.txt`） |

### 結論: **配管は全工程通った**

生成 → 距離ラベル＋前処理 → 学習 → 推論 → 採点 まで、armのデータで一度も止まらずに通り、
指標が読める形で出た。基準は最初から最後まで1バイトも変わっていない。

### 得られた数値（**卒論・発表に使わない。比較不能**）

自条件val（no_1r・80エポック）: 可聴検出率 95.2〜99.97%、方向誤差中央 1.0〜5.1°、
至近側(GT≤5m) 距離MAE 0.66m・中央0.24m、重大tier再現 68.1%。

**基準(w3)の確定値と並べてはいけない**理由が3つある:
1. 評価セットが違う（こちらはval、確定値はfold20の確定評価セット）
2. **エポック数が違う**（80 vs 100。時間制限で打ち切られたため）
3. 自条件val＝「その世界で学習しその世界で採点」であり、**主要評価のフル物理val
   （転移ギャップ）ではない**（確認runでは基準ルートに触れないため意図的に切った）

→ armの良し悪しはこの数値からは**何も言えない**。本番で §9.3 の主要評価を測ること。

## 5-bis. 【最終結果】4arm＋ラン揺れフロア（2026-08-16 19:18 完了）

当初は no_1r 1本の配管検証として始めたが、配管が確定したため4arm＋フロアまで実施した。
**ただし位置づけは確認runのまま**＝下記の数値は卒論・発表に使わない（→§0、§7）。

> ## ⚠️ 訂正あり（2026-08-17 自己監査で発覚・**下の数値をそのまま引用しないこと**）
>
> 下表の tier再現率は、**分母が「検出できたフレーム」だけ**で、検出に失敗したフレームが
> 分母から消えている（`_score_sde_dist.py` のペアリングで `if not preds: break`）。
> armごとに検出率が違うため、**arm間比較として成立していない**。
>
> 退避済みの値から再計算すると、**順位が逆転する**:
>
> | | 報告値（条件付き） | 再計算（絶対到達率・暫定） |
> | --- | --- | --- |
> | no_1r | −8.1pt | **−13.1pt（最大）** |
> | no_ground | −10.9pt（最大） | −10.1pt |
> | no_doppler | −3.3pt | −3.1pt |
> | no_airabs | −0.4pt | +0.2pt（差なし・変わらず） |
>
> no_1r は車検出率 98.19%（基準99.56%）と最も低く、重大帯で147フレームの検出失敗が
> 分母から落ちていた。**補正後は事前登録§9.4の予想「no_1rが最大」と一致する**ため、
> 「予想が外れた」という記述も撤回対象。
>
> 上の再計算は `paired × recall` から解析的に導いた**暫定値**。確定値は修正済み
> スクリプト（絶対到達率を出力・コミット 6edd8afd1）での再採点後に差し替える。
> **no_airabs が差なしという結論と、「この実験デザインは差を検出できる」という
> 確認runの結論は変わらない。**

> ### 【2026-08-17 再採点済み】確定値は `out/ablation_確認run_2026-08-16/rescore_2026-08-17/` にある
>
> 上の訂正注記のとおり採点に欠陥があったため、修正版で採点し直した。**下表は旧値。**
> **仮ablationであり卒論・発表には使わない**点は変わらない。
>
> **主要評価（事前登録§9.3のΔSELDスコア・フル物理val）** — フロア +0.0005 に対し:
>
> | arm | ΔSELD | 判定 |
> | --- | --- | --- |
> | **no_1r** | **+0.0308**（62倍） | 差あり・最大 |
> | **no_ground** | **+0.0124**（25倍） | 差あり |
> | no_airabs | +0.0010 | ほぼ差なし |
> | no_doppler | −0.0007 | **差なし** |
>
> **予想との答え合わせ: 2件的中・1件判定保留・1件外れ**
> （的中=no_1r最大・no_doppler微差／保留=no_airabsはフロアの2倍で判別不能／外れ=no_ground）。
> なお本表のSELD値は書き出しCSVから算出した別系統で、発表済みの0.087と同一視しないこと。
>
> **指標で結論が変わる点**: no_doppler は SELDスコアでは差なしだが、
> 通知の重大tier到達率では −3.0pt。**ドップラーは検出・方向には効かず距離推定に効く。**

### 主要評価: フル物理val（基準の世界で採点＝転移ギャップ、§9.3）

| 指標 | 基準 w3 | **フロア** | no_ground | no_1r | no_doppler | no_airabs |
| --- | --- | --- | --- | --- | --- | --- |
| 重大tier再現 | 69.1% | **±1.1pt** | **58.2%** (−10.9) | **61.0%** (−8.1) | **65.8%** (−3.3) | 68.7% (−0.4) |
| 注意tier再現 | 74.8% | ±0.9pt | 72.4% (−2.4) | **66.2%** (−8.6) | 72.9% (−1.9) | 75.0% (+0.2) |
| 安全tier再現 | 98.9% | ±0.1pt | 98.1% (−0.8) | 97.9% (−1.0) | 98.8% (−0.1) | 98.9% (±0.0) |
| 至近MAE(≤5m) | 0.69m | ±0.01m | **0.76m** (+0.07) | **0.83m** (+0.14) | **0.75m** (+0.06) | 0.69m (±0.00) |
| 至近中央(≤5m) | 0.25m | ±0.01m | **0.37m** (+0.12) | **0.44m** (+0.19) | **0.31m** (+0.06) | 0.25m (±0.00) |

**ラン揺れフロア** = 基準と完全同一条件・seedのみ変更（job834）で測定した run 間のブレ。
§9.3の事前登録どおり「**フロア以下の差は差なし**」と判定する。

### 効き目の順位

**地面反射 > 幾何減衰 > ドップラー ≫ 大気吸収（効果なし）**

### 事前登録（§9.4）との答え合わせ

| arm | 予想 | 実測 | 判定 |
| --- | --- | --- | --- |
| no_airabs | **差なし**（≲50mでは高々1-1.5dB） | 全指標フロア内 | **的中** |
| no_1r | 最大の差 | −8.1pt（2位） | 方向は的中・順位は外れ |
| no_doppler | 微差（\|ΔSELD\|≤0.005） | −3.3pt（フロアの3倍） | **やや外れ**（微差でなく有意差） |
| no_ground | el系はむしろ**改善** | **−10.9ptで最大の劣化** | **外れ** |

**外れた2つが研究上の中身**になる。特に地面反射は「仰角バイアスを生むだけ」と予想していたが、
実際は距離推定の手がかりとして効いていた可能性が高い（至近中央誤差が0.25→0.37mへ悪化）。

### 確認runとしての結論: **この実験デザインは信号を取れる**

差が出るarmと出ないarmが分かれ、しかも**「出ない」と事前に予想した条件（大気吸収）が
実際に出なかった**。4本とも大差が出れば計器の故障を疑うべきで、4本ともフロア内なら
スイッチ無効を疑うべきところ、そのどちらでもない。**計器に感度と特異度の両方がある**
ことの実証であり、本番を回す価値があると判断できる。

### ジョブ実績（全15本 COMPLETED・実測コスト）

| 工程 | 実績 |
| --- | --- |
| 生成（10,200本・12シャード） | 47分〜1時間55分（並列3本同時可・armにより変動。no_dopplerが最遅） |
| ラベル＋前処理 | 3〜28分 |
| 学習100ep＋推論2本 | **pro_6000: 47分** / **フルa100: 1時間23〜27分** / a100_3gスライス: 2時間30分 |
| 1エポックあたり | **pro_6000 31秒 / フルa100 52秒 / a100_3g 92秒** |

生成3本＋学習3本を並列で回し、**4arm＋フロアを約4時間で完了**（15:30→19:18）。

### 基準の無変更（最終確認）

7パスすべて実行前と完全一致。証拠= `out/audit_logs/ablation_final_fingerprint_2026-08-16.txt`
（データセット3種・datasets_v12・**_hdf5**・基準run・初期重みckpt）。

### 途中で起きたこと

- **監視スクリプトの誤判定**: ssh接続が切れたとき、`squeue` の空出力を「ジョブ終了」と
  誤って解釈した。**ssh成功時にのみ判定する**よう修正済み。Slurmジョブ自体はssh切断の
  影響を受けずに走り続けていた
- **pro_6000のGPU枯渇**: 4枚とも長期ジョブ（16日連続・3日枠×2）が占有。生成はGPU不要だが
  QoSが最低1GRESを要求（`QOSMinGRES`）するためCPU単独では投げられず、a100のMIGスライスへ
  逃がして待ち時間ゼロにした（2.5時間短縮）

## 6. 本番への申し送り

**転記済み**: 上記の地雷3件・投入コマンド・ガードの扱いは、本番担当者が読む
`アブレーション実験計画_2026-07.md` **§9.8** に転記した。本番はそちらを参照する。

- [x] §4の地雷3件を本番手順書（§9.8）に転記
- [x] ガードと `ABL_FULLVAL` の扱いを §9.8 に明記
- [ ] 本番は §9 の事前登録に沿う。土俵を変えるなら §10 を新規に立てる（§9.7の規約）
- [ ] 確認runの成果物（`*_abl_no_1r`）は本番開始前に削除する
