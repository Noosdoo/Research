# 確定評価v2 「束」の設計案（下書き・2026-09-03）

v2 = 卒論まで最後の学習データ・モデル・通知規則の更新（第10回監査条件: v2で打ち止め・v1と併記・1回だけ）。
この文書は **11月の v2 事前登録の材料**。10月末に実録の結果を入れて確定し、テンプレート
（`確定評価v2_事前登録テンプレート_2026-08-30.md`）を埋めて発効させる。

## 0. 決まっていること（2026-09-03 時点）

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| 通知規則 | **v4.3**（v4.2＋routeC 3点）＋警告音 hold | v4.3 宣言§3・本人「常にすべて v4.3」 |
| 採点器 | 確定評価= v33系（車ごと方位帰属・v1と同一）。開発用 val の選定は方位帰属版 | 第11回監査 論点6（訂正版）・本人「B」 |
| 基準学習のレシピ | **現行のまま**（warm-start・batch8・lr3e-4・StepLR60・100ep） | ④ Stage B: 6arm 全部フロア内 |
| 学習データの土台 | **v13 レシピ**（S1〜S4・可聴ゲート・歩行70%・雨20%） | v13設計書§5: 旧分布同等・雨/遠方サイレン大幅改善・本人試聴OK |
| 鳴らしすぎ対策 | 規則側は入れない。**機器側で束ねる**（同段の再トリガ1秒以内は延長） | v4.4 宣言§4 |
| 実施回数 | fold20 に1回だけ。v1（73.0%）と併記、悪化しても差し替えない | 第10回監査 |

## 1. 学習データ v2 = v13 ＋ 調整（再生成は11月に1回）

| # | 項目 | v13 での値 | v2 案 | 決める時期・根拠 |
| --- | --- | --- | --- | --- |
| D1 | サイレンの開始距離の混合 `SIREN_MIX` | near 0 / far 0.8 / recede 0.2 | **near 0.5 / far 0.3 / recede 0.2** | 近接例の半減で 0-50m が −2pt（v13§5.2）。実装済み（既定は v13 と同一） |
| D2 | 雨のレベル（dB(A)） | 仮置き 45-50/50-58/60-68 | 10月の騒音計測定値に置換 | 出典が無い（監査 論点3） |
| D3 | 雨の比率 | 20% | 20%（変更なし） | 降水日数32%・時間割合1割＋フロア |
| D4 | 静止:歩行 | 30:70 | **10月の連続録音100分の実測**で更新（±10pt以内なら据え置き） | 監査 論点4 |
| D5 | 警告音の可聴ゲート閾値 | SNR_A ≥ 0 dB | 据え置き | 0dB未満は0.6%・v13ft は −3〜0dB で91%検出（根拠あり） |
| D6 | 至近帯の距離バイアス | — | 学習側で対処するか検討: 距離損失の至近重み or 較正 | ②閉じで判明（因果モデルは0.5-1mで+25%） |
| D7 | 風（W1） | なし | 10月の実録で残留量を測って判断 | 候補リスト§2.5 |
| D8 | 本数 | 7,200 | 7,200（据え置き。増量の価値は低い＝本人議論 2026-09-02） | — |
| D9 | テクスチャ（⑫a） | — | 10月の実録ABで特定してから | — |

## 2. 学習レシピ v2

| 段 | v1（現行） | v2 案 | 状態 |
| --- | --- | --- | --- |
| 基準学習 | init=v12_init_from_run3ep84・batch8・lr3e-4・StepLR60・100ep | **同じ** | 確定（④） |
| 因果ft | lr3e-4・REPEAT2・100ep・捕捉率でckpt選択 | **lr1e-4・150ep**・REPEAT は追試結果で決定・**ckpt選択は至近帯の距離誤差** | 追試3本（別seed/REPEAT8併用/150ep）を実行中 |
| 初期重み | 3段連鎖（run2→run3→w3） | 同じ。SHA256 と系譜を登録文に記録 | 確定（scratch は劣る） |

⚠️ v2 のデータで基準学習から回すので、因果ftの起点は v2 の基準学習 ckpt。

## 3. 評価枠（v2 で追加するもの）

- 確定評価: fold20 core 1,200本（不変・1回）。報告は v1 と同じ採点器で直接比較
- 開発用 val（fold2 系・v2 文法・雨20%）: 方位帰属で採点、雨・文法・静止/歩行で層別
- **サイレン距離帯別再現率**（0-50 / 50-100 / 100-200 / 200-500 / 500m〜）を標準表に
- **長尺セット（新設）**: 60秒×N本（幹線・歩行・車3〜6台）。流れモード（同じ側で中がK回→抑制）を
  ここで初めて評価できる。実録の連続録音100分と対にする
- 実録（10〜11月）: 210本＋連続100分。ablation共通110本は全armに通す（既定方針）

## 4. 通知層 v2（規則は v4.3＋hold で固定。以下は機器側の実装規約）

- 束ね: 同じ手（方向）・同じ段の再トリガが1秒以内なら振動を延長（デモv2で体感確認中）
- 段: 強=4連打・中=2発・警告=単発（離散）。連続モード（緊急度で振幅）はデモで比較して採否
- 方向: 首元4〜6方向は隣接振動子への按分（Joy-con 4本で予備実験）

## 5. 時系列

| 時期 | やること |
| --- | --- |
| 9/15 | 相談会（機材購入・規程・騒音計・提出期限） |
| 9月中 | ④追試の結果でレシピ確定／長尺セットの生成器（60秒）を作る／v2の plan 生成器に D1〜D5 のパラメータ化 |
| 10月 | 実録（収録・注釈）→ D2/D4/D7/D9 の判断材料 |
| 10月末 | **v2 の束を確定**（この文書を更新）→ 事前登録テンプレートを埋めてコミット（発効） |
| 11月 | 生成 → 基準学習 → 因果ft → 確定評価 1回 → 結果 |
| 12月 | 実録の採点・分析、卒論の材料化 |

## 6. 未決（本人判断が要るもの）

1. D6 至近帯の距離バイアスを学習側で扱うか（損失の重み付け＝レシピ変更＝追試1本が要る）
2. 連続モード（緊急度）を採るか離散のままか（デモの体感で）
3. 流れモードを v2 の規則に入れるか（長尺セットの結果次第。入れるなら宣言→掃引の作法）
