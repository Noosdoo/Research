# 確定評価v2 「束」の設計案（下書き・2026-09-03）

v2 = 卒論まで最後の学習データ・モデル・通知規則の更新（第10回監査条件: v2で打ち止め・v1と併記・1回だけ）。
この文書は **11月の v2 事前登録の材料**。10月末に実録の結果を入れて確定し、テンプレート
（`確定評価v2_事前登録テンプレート_2026-08-30.md`）を埋めて発効させる。

## 0. 決まっていること（2026-09-03 時点）

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| 通知規則 | **v4.3**（v4.2＋routeC 3点）＋警告音 hold | v4.3 宣言§3・本人「常にすべて v4.3」 |
| 採点器 | 確定評価= v33系（車ごと方位帰属・v1と同一）。開発用 val の選定は方位帰属版 | 第11回監査 論点6（訂正版）・本人「B」 |
| 基準学習のレシピ | **現行のまま**（warm-start・batch8・lr3e-4・StepLR60・100ep） | ④ Stage B: 6arm 全部フロア内 |
| 学習データの土台 | **v17b レシピ**（v13 の束 S1〜S4・歩行 70%・雨 20% ＋ D10 至近低速 ＋ D13 高さ 1.4〜2.1 m の幅 ＋ 高さ増強（各場面 2 高さ）＋ **装着高さをモデル入力（FiLM・学習時ゆらぎ 0.10 m）**・水平距離ラベル） | 本人決定 2026-09-06 15:04。v17 宣言 §8: v15 val 7.8%（v16 9.9・ft2 9.4）、±10 cm の高さ誤入力で 8.3〜9.6%。実用要件 = 装着高さを実測して ±10 cm 以内で設定 |
| 鳴らしすぎ対策 | 規則側は入れない。**機器側で束ねる**（同段の再トリガ1秒以内は延長） | v4.4 宣言§4 |
| 実施回数 | fold20 に1回だけ。v1（73.0%）と併記、悪化しても差し替えない | 第10回監査 |

## 1. 学習データ v2 = v13 ＋ 調整（再生成は11月に1回）

| # | 項目 | v13 での値 | v2 案 | 決める時期・根拠 |
| --- | --- | --- | --- | --- |
| D1 | サイレンの開始距離の混合 `SIREN_MIX` | near 0 / far 0.8 / recede 0.2 | **near 0.5 / far 0.3 / recede 0.2** | 近接例の半減で 0-50m が −2pt（v13§5.2）。実装済み（既定は v13 と同一） |
| D2 | 雨のレベル（dB(A)） | 仮置き 45-50/50-58/60-68 | 10月の騒音計測定値に置換 | 出典が無い（監査 論点3） |
| D3 | 雨の比率 | 20% | 20%（変更なし） | 降水日数32%・時間割合1割＋フロア |
| D4 | 静止:歩行 | 30:70 | **10月の連続録音100分の実測**で更新（±10pt以内なら据え置き） | 監査 論点4 |
| D5 | 警告音の可聴ゲート閾値 | SNR_A ≥ 0 dB | 据え置き | 0dB未満は0.6%・v13ft は −3〜0dB で91%検出（根拠あり） |
| D6 | 至近帯の距離バイアス | — | 学習側で対処するか検討: 距離損失の至近重み or 較正 | ②閉じで判明（因果モデルは0.5-1mで+25%） |
| D7 | 風（W1） | なし | 10月の実録で残留量を測って判断 | 候補リスト§2.5 |
| D8 | 本数 | 7,200 | 7,200（据え置き。増量の価値は低い＝本人議論 2026-09-02） | — |
| D9 | テクスチャ（⑫a） | — | 10月の実録ABで特定してから | — |
| D10 | **至近・低速の層**（2026-09-03 本人「実装してください」→ **実装済み v14**） | 車ありクリップの全フレームのうち ≤1.5m 1.88%・≤1.0m 0.45% | critical（住宅街/日常）の 50% を徐行 5〜15 km/h・最接近 0.6〜1.2 m・音量 −6〜−10 dB に置換 → **≤1.5m 2.95%・≤1.0m 0.81%**（置換 640 本は 3.9→12.2 フレーム/本） | `step10_v14_plan.py`／`step11_v14_render.py`／`_run_v14_gen.py`。至近は物理的に短い（速い車は 0.2 秒）ので割合の上限は低い。つまみは CLOSE_SLOW_FRAC（1.0 で ≤1.5m ≈4%）と速度帯。試聴 A/B = `out/v14_proto_listen/`（本人確認待ち）。学習して「旧分布同等」を確認するのは v2 の束で |
| D11 | **車を複数点の音源にする**（候補） | 車 = 1 点（高さ 0.8〜1.3 m）。距離 = マイク(1.5 m)からその 1 点まで | 前輪/エンジン・後輪の 2〜3 点＋距離 = 車体の最近接点 | 至近では車体が広がって見える（1 m 横なら ±60°）。実録の距離定義（横距離＝車体側面まで）と揃える。10月の実録 AB で必要性を見てから |
| D13 | **マイクの高さ**: 1.4〜2.1 m の幅で学習し、**装着高さをモデルに入力**する（v17b）。実録の記録紙に装着高さの実測値（cm）を必須にし、推論時にそのまま入力 | 本人決定 2026-09-06。経緯: 固定学習（v15b）は 20 cm ずれで崩壊、幅だけ（v15/v16）は 9.9〜10.6%、高さ入力（v17/v17b）で 7.3〜7.8% |

## 2. 学習レシピ v2

| 段 | v1（現行） | v2 案 | 状態 |
| --- | --- | --- | --- |
| 基準学習 | init=v12_init_from_run3ep84・batch8・lr3e-4・StepLR60・100ep | **同じ** | 確定（④） |
| 因果ft | lr3e-4・REPEAT2・100ep・捕捉率でckpt選択 | **lr1e-4・150ep**・REPEAT は追試結果で決定・**ckpt選択は至近帯の距離誤差** | 追試3本（別seed/REPEAT8併用/150ep）を実行中 |
| 初期重み | 3段連鎖（run2→run3→w3） | 同じ。SHA256 と系譜を登録文に記録 | 確定（scratch は劣る） |

⚠️ v2 のデータで基準学習から回すので、因果ftの起点は v2 の基準学習 ckpt。

## 3. 評価枠（v2 で追加するもの）

- 確定評価: fold20 core 1,200本（不変・1回）。報告は v1 と同じ採点器で直接比較
- 開発用 val（fold2 系・v2 文法・雨20%）: 方位帰属で採点、雨・文法・静止/歩行で層別
- **サイレン距離帯別再現率**（0-50 / 50-100 / 100-200 / 200-500 / 500m〜）を標準表に
- **長尺セット v1（新設・完成）**: 60 秒 × 400 本（fold40/41、交通量 3 層）。流れモードの掃引は不採用で決着（§6.1 3′）。
  v2 モデルでは **誤った中・強の率と同じ車への再発火率**をここで測る（実録の連続録音 100 分と対にする）
- 実録（10〜11月）: 210本＋連続100分。ablation共通110本は全armに通す（既定方針）

## 4. 通知層 v2（規則は v4.3＋hold で固定。以下は機器側の実装規約）

- 束ね: 同じ手（方向）・同じ段の再トリガが1秒以内なら振動を延長（デモv2で体感確認中）
- 段: **連続モード採用（2026-09-03 本人）**: 緊急度 0..1 で振幅・周波数を連続に変える。警告音は単発のまま。離散（強=4連打・中=2発）は比較用に残す
- 方向: 首元4〜6方向は隣接振動子への按分（Joy-con 4本で予備実験）

## 5. 時系列

| 時期 | やること |
| --- | --- |
| 9/15 | 相談会（機材購入・規程・騒音計・提出期限） |
| 9月中 | ④追試の結果でレシピ確定／長尺セットの生成器（60秒）を作る／v2の plan 生成器に D1〜D5 のパラメータ化 |
| 10月 | 実録（収録・注釈）→ D2/D4/D7/D9 の判断材料 |
| 10月末 | **v2 の束を確定**（この文書を更新）→ 事前登録テンプレートを埋めてコミット（発効） |
| 11月 | 生成 → 基準学習 → 因果ft → 確定評価 1回 → 結果 |
| 12月 | 実録の採点・分析、卒論の材料化 |

## 6. 未決（本人判断が要るもの）

1. D6 至近帯の距離バイアスを学習側で扱うか（損失の重み付け＝レシピ変更＝追試1本が要る）
2. 連続モード（緊急度）を採るか離散のままか（デモの体感で）
3. 流れモードを v2 の規則に入れるか（長尺セットの結果次第。入れるなら宣言→掃引の作法）

### 6.1 判断（2026-09-03 13:24・本人）と、それを受けた作業

| # | 判断 | 受けて決まること |
| --- | --- | --- |
| 1 | **B 後から補正する**（学習は変えない） | v2 の手順に「距離の補正表」を追加。**手順だけ事前登録**し、数値は v2 モデルで作り直す（fold32 で作成 → fold30 で確認 → 確定評価の前に固定）。試作は下記 |
| 2 | 連続寄りだが、同じ車を追えない問題の対応付けが不安（未確定） | デモに **J: 系列切替の合図**（方位が 0.3 秒以内に 60° 跳んだら短い2連＋弱いところから立ち上げ直す）を追加。「追えない」を「別の車が来た」の合図に変える案。体感してから決める |
| 2′ | **本人判断（2026-09-03 20:07）: 連続モード（G）を採用** | デモ v2 の既定を G=ON に。段階の振動（強=4連打・中=2発）は G=OFF で残す。機器（首元）の実装は緊急度 0..1 → 振幅・周波数の連続変換＋系列切替の合図。卒論の評価指標は段（強到達・安全抑制）のまま |
| 3 | **A 流れモードを v2 の規則に入れる**（長尺セットを作る） | 長尺セットの設計: **60 秒 × 200 本（選定用・fold40 系）＋ 200 本（検証用・fold41 系）= 400 本**。内訳は幹線・歩行・車 3〜6 台/分 を 70%、住宅街・静止を 30%、至近を含む本を 20% 以上。指標 = 注意の回数/分・抑制した注意のうち後で至近になった車の割合（害）・強到達（不変であること）。作法は v4.3 と同じ（先に宣言 → 掃引 → 別データで確認）。9月中に生成器の 60 秒対応 → 宣言 → 掃引 |
| 3′ | **結果（2026-09-03 19:40）: 流れモードは不採用** | 長尺セット v1（60 秒 × 400 本）を作り fold40 で 8 構成を掃引 → 注意/分の減りは 2〜4%、抑えた分の 50〜67% が害（基準: −30%・≤2%）。理由: 注意は 1.25 回/分しか出ず「連続」ではない。鳴らしすぎの正体は **安全な車への誤った中・強（中の 68%）** と **同じ車への再発火（25〜49%）**。v2 の規則は v4.3＋hold のまま。長尺セットは v2 モデルの評価枠に残す（`md/results/通知v4.5_結果_流れモード不採用_2026-09-03.md`） |
| 3″ | **車の hold（v4.6/4.6b/4.6c）も不採用で確定（2026-09-03 20:30）** | 強も抑える版は害 7〜9%・強到達 −8〜−19pt。中だけ抑える版は害 0〜1%・強到達不変だが中の再発火の減りが fold40 −48% / fold41 −37% で、改定後の基準（−40%）にも届かず。**v2 の規則は v4.3＋警告音 hold のまま**。実録の連続 100 分で鳴る回数を測ってから v2 後に再検討（`md/results/通知v4.6_結果_車のhold_2026-09-03.md`） |

**補正表の試作**（`scripts/_dist_calib_proto.py` → `out/hp_sweep/dist_calib_proto.json`。現行モデル ft2 e079・fold32 で作成）:
「モデルが言った距離のビン（0.25 m 刻み）→ 実際の距離の中央値」の単調な表。5 m 超はそのまま。
1.5〜3 m の予測を 0.05〜0.10 m 縮めるだけの小さな表だが、別データ（fold2 val）で **至近捕捉 61.7→69.1%・誤捕捉 0.04→0.05%
（不変）・距離誤差 9.6→9.7%（不変）**。ただし 0.5〜1 m の「遠く言う」癖そのもの（+18%）は直らない（モデルが 1 m 未満を
ほぼ出さないため、表では戻せない）。→ 補正表は「1.5 m のしきい値をまたぐ取りこぼし」対策として効く。本数の少ない
ビン（<30 ペア）は恒等にする。**規則の前段に置く**ので v4.3 の閾値はそのまま。

長尺セットの交通量（2026-09-03 本人指摘で修正）: 「3〜6 台/分」は **1 分間に通る台数**で同時ではない（同時は最大 2〜3。
モデルは同クラス 3 系列まで＝mACCDOA の 3 トラック）。実際の幹線の歩道は 10 台/分を超えることもあるので、
交通量を **2〜4 / 4〜8 / 8〜12 台/分 の 3 層**にして流れモードの効きを密度別に見る。場面は幹線の歩道（歩行）70%・
幹線の静止（信号待ち・バス停）15%・住宅街（1 台/分以下・負例）15%。「歩道近くを歩く以外で車が次々通ることはない」は
そのとおりで、流れモードの対象はその場面だけ。他の場面では何も起きないことの確認が 30% の役目。

長尺セット 400 本の根拠: 1 本に車 3〜6 台 → 200 本で約 900 台の通過。「害」（抑制した注意のうち後で至近になる割合）を
±2pt で見積もるのに 1,000 件規模が要る。生成は 10 秒換算 2,400 本分（A100 ノードで約 40 分・pro_6000 で約 15 分）、
因果推論は同じく約 10 分。作ってから足りなければ倍にする。
