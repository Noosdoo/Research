# 通知 v4.3 検討の事前宣言（2026-09-03 01:30・結果を見る前に固定）

発端= `引き継ぎ_通知層v4.3_検討依頼_2026-09-03.md`（9/15スライド作成チャットからの依頼）:
fold31 の型別表で **安全(>3.2m)×すり抜け型 n=501 の「強」が v4.1 61件 → v4.2 114件** に倍増。
v4.2 宣言 §7.4「それ以上の再試行はしない」に対し、**新しい宣言を立てて再開する**形を取る
（経緯はここに明記。v4.2 宣言は書き換えない）。

## 0. 設計診断で分かったこと（宣言前・fold31 の既存予測を読んだだけ。選定ではない）

1. 部品別の帰属（同表を部品OFFで再計算）: cs1.3→1.0 で 114→91、routeC OFF で 114→90、link OFF で 116。
   → 倍増は **cs と routeC が半分ずつ**。どちらも重大×対向型の強を約16件ずつ減らす代償つき
   （= 単純に戻すとv4.2の利得を食う）
2. 誤った強の「最初の強フレーム」の個票（`scripts/_notify_v43_diag.py` → out/notify_v43_diag/）:
   cpa経路76件は **推定距離 4.7m に対し GT距離 14.8m**（3倍のずれ）。rc経路38件は d 13m・|adot| 0.05。
3. **114件のうち104件は複数車クリップ（n_car=2〜3）**。同時刻に同クラスの別の車が
   ≤3.2m に29件・≤8m に34件いる。つまり **強警告の多くは「別の近い車」に対して正しく出たもの**で、
   型別表の outcome() が「窓内の強を全部そのイベントの結果」と数えるため、安全な車にも
   同じ強が付いている（**帰属の artifact の疑い**）。採点器 score() も窓内の先着 fire を
   イベント順に消費する貪欲マッチで、複数車クリップでは同種のずれが起こりうる。

## 1. 方針（2段階。段階2は段階1の結果次第）

### 段階1: 帰属の修正（規則は変えない・評価の測り方だけ）

- **方位で fire を車に紐づける**: fire (frame j, 方位 az) を、同クラスで j の前後 ±0.5s に存在する
  GTイベントのうち **方位差が最小（≤30°）** のものに帰属する。該当なしは「幻影」として別計上。
- これで fold31（診断用・v4.2選定に使用済み）と fold30（v4.2検証用）の型別表を v4.1/v4.2 で再計算する。
- **判断基準（先に固定）**: 方位帰属後の「安全×すり抜け型の強」について
  - v4.2 − v4.1 ≤ **+15件**（fold31）かつ fold30 でも同傾向 → **倍増は帰属の artifact** と結論。
    規則は変えず、型別表と q2 回答表を方位帰属版に差し替えて報告（旧版は「窓帰属」として併記）
  - それより多く残る → 段階2へ
- 副産物として score() の貪欲マッチも方位帰属版を作り、v4.1/v4.2 の主要指標（強到達・抑制）が
  どれだけ動くかを併記する（**採用済み数値の差し替えはしない**。報告に「帰属方式」を明記して併記）

### 段階2: 規則の微修正（段階1で本物の誤強が残った場合のみ）

- 新val **fold32**（新シード帯・1,800本・fold30/31と同構成）を生成し、ft2 の因果推論を当てる。
  fold31 は診断用、fold30 は検証用（**2回目の使用**であることを開示）。fold20・実録は不使用
- 部品（結果を見る前に固定・各1〜2水準）:
  - P1 **方位整合ゲート**: cpa経路の強成立時に、方位由来の最接近 d·sin(∠(v,r)) が cs の2倍を超える
    （=方位が「横を通る」と言っている）なら中に降格
  - P2 routeC の距離ゲート dn 15 → 12 m
  - P3 routeC の |adot| しきい値 0.10 → 0.07 rad/s
- 選定: v4.2 と同じ偶奇ホールドアウトの min-max 規則。**主目的= 安全×すり抜け型の強を減らす**、
  制約= 重大の強到達が両半分で v4.2 − 1pt 以上、安全×対向型の無発火が v4.2 − 5pt 以上、
  全体の安全抑制が v4.2 − 1pt 以上。採用条件= fold30 で同方向（強 −1pt 以内・誤強 −20%以上）
- cs=1.3 のグリッド端は動かさない（動かすなら追記宣言）

## 2. 触らないもの

fold20（確定評価）・実録・v4.2 宣言本文・採用済み数値（`通知v4.2_採用結果まとめ`）。
再生成・再学習を要する案は範囲外（引き継ぎ §3-6）。

## 3. 結果（2026-09-03 04:10・fold32 掃引＋fold30 検証）

正= out/notify_v43_sweep/sweep_report.md（36構成の全数値）／ winner.json ／ fold30_table/q2_table_attrib.md

### 段階2の結果 — **採用**（宣言の採用条件をすべて満たした）

- fold32（job3872・15G帯・ft2因果推論）: 制約を両半分で満たした構成 **6/36**。勝ち= **#11
  `dn12 + adot_th0.07 + rc_brg_win13`**（強の方位ゲート sa は無し＝∞）。
  誤強 max(半分) **21 → 11**、強到達 86.0/87.3 → 86.4/87.6、抑制 74.9/75.8 → 77.5/76.6、安全×対向型 無 72/58 → 75/58
- **fold30 検証（2回目の使用）**: 誤強（方位帰属）**35 → 17（−51%）**、強到達 **89.3 → 89.3（±0）**、
  抑制 **76.9 → 78.6（+1.7）**、安全×対向型 無 71 → 71 → 採用条件 ✅
- 型別表（fold30・方位帰属）: 重大×対向型の強 386→387（不変）、注意×すり抜け型の強 159→132（横を通る車が
  「中」に寄る＝意図どおり）、安全×すり抜け型の強 35→17
- 強の方位ゲート（P1' sa=0.25/0.15）は全構成で強到達の制約を割った（不採用）。効いたのは
  **routeC の3点（距離ゲート12m・定方位しきい値0.07・方位窓13フレーム）**＝機序の診断どおり

### v4.3 の定義

v4.2 採用構成 ＋ `dn=12.0, adot_th=0.07, rc_brg_win=13`（`step12_notify_v43.py` の Cfg43）。
他のしきい値（cs1.3/cm1.6・brg5・mn4/4・link）は不変。

### 数値の扱い（本人判断待ち）

- 宣言上は採用成立。**正本（通知v4.2_採用結果まとめ）の差し替えは本人判断**（引き継ぎ§6「勝手に差し替えない」）。
  推奨= v4.3 を現行の正にする（全指標で v4.2 以上、強到達は同一）。並べるときは v4.2 をラベル付きで別枠に
- **帰属方式**（窓 vs 方位）は第11回監査の論点。v4.3 の採否は窓帰属の制約で判定しているので方式の
  選択に依存しない

### 3行要約（引き継ぎ§5-4）

1. 「安全×すり抜け型の強 61→114」の大半は帰属の artifact（別の近い車への正しい強）。方位帰属では 5→24
2. 残る本物の誤強は routeC の方位分解能不足が主因 → routeC だけ距離12m・0.07rad/s・13フレーム窓に
3. v4.3: fold30 で誤強 35→17（−51%）、強到達 89.3→89.3、安全抑制 76.9→78.6。採用条件クリア

## 追記1（2026-09-03 01:55・fold32 は生成中で予測は存在しない＝事前性維持）: 段階1の結果と段階2の部品の確定

### 段階1の結果（方位帰属・`scripts/_notify_v43_attrib.py` → out/notify_v43_diag/fold31, fold30）

| | 安全×すり抜け型 強（窓帰属） | 同（方位帰属） | 判定 |
| --- | --- | --- | --- |
| fold31 | v4.1 61 → v4.2 114 | v4.1 **5** → v4.2 **24**（+19） | +15 を超える → 段階2へ |
| fold30 | v4.1 62 → v4.2 118 | v4.1 **4** → v4.2 **35**（+31） | 同上 |

- 倍増の**大半は帰属の artifact**（近い別の車に正しく出た強が安全な車にも付いていた）。
  残る本物の誤強は 24〜35件/約500（5〜7%）で、v4.1 比 +19〜31件 → 宣言の基準どおり段階2へ
- 副産物: 方位帰属で主要指標が大きく動く（fold30 v4.2: 強到達 89.3→80.2%、安全抑制 76.9→89.4%）。
  窓帰属の採点器は複数車クリップで強到達を過大・抑制を過小に出している。**採用済み数値は差し替えず**、
  第11回監査と確定評価v2の事前登録で「帰属方式」を決める論点として扱う

### 残る誤強の機序（方位帰属後・fold31 27件 / fold30 40件）

- **rc経路が過半（15/27, 30/40）**: 距離12.5m・CPAの2.5s前・GT最接近5〜6m・ddot −3m/s（遅い車）。
  測定|adot| 0.05〜0.07 に対し GT 0.175 → **整数度の方位を5フレーム窓で微分すると分解能が
  0.044rad/s しかなく**、遅い車の「横を通る」動きが 0.10 の閾値以下に潰れる
- **cpa経路（12/27, 10/40）**: 推定距離 6〜7m に対し GT 13.6〜14.3m（距離を半分に見誤る）。
  d_cpa ∝ d² なので 4倍小さく出て強になる。測定|adot| は 0.05〜0.21

### 段階2の部品（§1 の P1 を差し替え・P4 を追加。fold32 の予測を見る前に固定）

| 部品 | 水準 | 狙い |
| --- | --- | --- |
| P1' `strong_adot_max` | ∞(なし) / 0.25 / 0.15 rad/s | cpa経路の**強**は「方位がほぼ動かない」ときだけ（強=衝突コース、横を通るなら中）。Q2の約束の明示形 |
| P2 `dn` | 15 / 12 m | routeC の距離ゲートを詰める |
| P3 `adot_th` | 0.10 / 0.07 rad/s | routeC の定方位判定を厳しく |
| P4 `rc_brg_win` | 5 / 9 / 13 フレーム | **routeC だけ**方位窓を長くして分解能を上げる（cpa経路の窓5は不変＝早さを保つ） |

グリッド= 3×2×2×3 = 36 構成（v4.2採用構成 = (∞,15,0.10,5) を含む）。

### 選定規則（fold32・偶奇ホールドアウト・min-max）

- 目的= **安全×すり抜け型の強の件数（方位帰属）を最小化**
- 制約（両半分で満たす）: 強到達（窓帰属・採用済み採点器）≥ v4.2 −1pt／安全抑制 ≥ v4.2 −1pt／
  安全×対向型の無発火 ≥ v4.2 −5pt
- 勝ち= 制約を両半分で満たす構成のうち max(半分ごとの誤強件数) が最小。同点は min(強到達) 最大
- 採用条件（fold30 で検証・2回目の使用を開示）: 誤強（方位帰属）が v4.2 比 **−20%以上**、
  強到達 ≥ v4.2 −1pt、安全抑制 ≥ v4.2 −1pt。満たさなければ**採用なし**と記録して終える
