# v12設計書 — リアリズム強化（2026-08-05 05:10 起草）

議題= md/design/v12リアリズム_議題_2026-08-05.md の6本を設計判断に落とす。
上位方針:「新しい耳」格上げ済み・難聴者=第1対象維持・順序は v12 → ablation本番 → 実録9月。

## 0. 基本構造（議題5: バージョン規律）

- **v11は凍結**（正典維持）。v12は新データセット名・新フォルダ
- **同一plan・同一seedの全量再生成**（E1a方式の全面版）: 幾何・軌道・クラス配置は
  v11と1対1対応のまま、音源・背景の「音」だけが変わる → ペア比較可能
- ラベル・マスクは再生成に伴い自動更新（音が変わればSNR・可聴性は変わるのが正
  — GT幾何は不変）
- キックボードは**追加行**方式（既存7,200行は不変更のまま新規クリップを追加）
  → 採用を後から取り消しても本体に影響しない
- 決定論の受け入れ検査: D2手順で1クリップ再生成ビット一致を出荷条件にする

## 1. V12-0: レンダのサーバー移植（前提基盤、v12とablation両方が依存）

- 全量再生成が今後最低5回発生（v12×1 + ablation×4）。ローカル26h/回は非現実的、
  サーバー並列で1〜2h/回にする
- 移植物: outdoor_seld_e2e/src・scripts・plan CSV・dynamic_sound パッケージ
  （fastsimがISO 9613係数関数をimport。物理実装のフォークはしない=そのままコピー）
- **受け入れ検査（必須）**: サーバーで fold2_room1_mix0001 を再生成し、ローカル正と
  flac sha256一致（07246271f794…）を確認してから量産に使う
- 運用: データセットはサーバー側に生成し学習までサーバーで完結。ローカルへは
  採点・試聴に必要な分（メタデータ全部＋音声は必要セットのみ）を選択転送

## 2. V12-1: 音の実物化（議題1）

### 2a. 背景騒音: 完全ピンク → 都市暗騒音スペクトル
- 根拠: 都市の環境騒音は交通由来で**約60Hz峰**を持つ（Applied Acoustics実測、
  Baltimore/Waikiki 1/3オクターブ）。現行ピンクはオクターブ等パワー=低域が現実比
  10〜20dB軽い（E0解釈制限の正体）
- 実装: noise.pyに**v12形状**（63Hzオクターブ峰、峰から上へ約-4dB/oct、20Hz未満遮断は
  維持）を追加。dB(A)較正・拡散FOA化・レベル帯40〜65dB(A)は不変更
- 検品: 生成背景のオクターブ実測が目標形状±3dB
- 効果の見どころ: 低域SNRが現実化→E0で測った「低周波の寄与」が現実条件で再検定される

### 2b. 静音EV: インバータトーンの追加（「新しい耳」の先取り）
- 現行: EV=単に静かな車（N2検出20%）。実物のEVはPWM由来トーンを発する
  （5〜15kHz、文献= 不可聴帯域リサーチ§2）
- 実装: engine系に **EVバリアント** = タイヤ音主体 + ①固定PWMトーン（8〜10kHzで
  クリップ毎抽選・±ゆらぎ）②速度比例モータ次数トーン（1〜4kHz帯）。42Hzエンジン列は
  無し。レベルは「トーンが全体dB(A)の中で卓越比 数dB」を文献値から較正（実装時に確定）
- 適用: 車のうち**15%をEV指定**（決定論抽選）。N2（静音EV苦手セット）の車もEV音源化
- 検品: スペクトルに5〜15kHz帯トーン卓越が出ること・dB(A)規約不変
- 事前登録の見どころ: N2検出率 20% → 改善（数値ゲートは学習前に確定）

### 2c. 大型車の標準混入
- E1aのデルタ（63Hz帯強化・dB(A)不変・検品済み）を**恒久仕様として車の30%に適用**
- 根拠: 実街路の車種構成の再現（E1-fullで性能への害が無いことは確認済み）。
  ablationの土俵を現実に近づける意味を持つ

### 2d. 「合成感」の分解（本人の7月指摘への対処）
- 仮説3つ: ①背景が特徴のないピンク（→2aで解消） ②残響ゼロの乾いた音場
  （→壁面反射はablation期の物理追加として設計、v12では見送り） ③音源の定常性
  （→エンジンAM・ゆらぎは既にあり。タイヤ音レベルの速度連動を追加する小改良のみ）
- **判定は本人の試聴A/B**（v11 vs v12の同一クリップ比較を listen フォルダに用意）を
  検品ゲートに含める

## 3. V12-2: シチュエーション実物化（議題2、最小セット）

- **車列化**: 複数車クリップ（2,520本）の2台目以降を同一レーン追従（車間 実勢分布）に
  変更するオプション。独立軌道との配分は 50:50（多様性維持）
- **停止→発進**: v10苦手セットの停車→急発進パターンを学習側にも混ぜる（車の5%）
- マイクの方向転換・壁面反射は**v12では見送り**（ラベル規約・物理追加が絡むため
  ablation期に判断。スコープ肥大防止）

## 4. V12-3: キックボード新クラス（議題4、採用推奨・追加行方式）

- 判断: **採用を推奨**。理由=①可聴帯域内で完結 ②「新しい耳」の物語の現行世代での
  体現 ③追加行方式なら本体無傷で後退可能 ④この窓を逃すとablation後は追加不能
- 音源モデル: PWMモータトーン（6〜10kHz、速度連動）＋転がり音（広帯域、小レベル）。
  レベル・速度（3〜7m/s、歩道/車道）は実測文献を実装時に1本当てて較正
- 規模（最小）: 学習+900クリップ・val+225・評価専用+300（苦手型150+基本150）。
  クラス数6→7（ヘッド変更・全再学習はv12でどのみち発生するので限界コスト小）
- 撤退条件: V12-1検品までにキックボード音源の較正根拠が集まらなければ次版送り
  （本体v12はそのまま出荷）

## 5. V12-4: 評価データ拡充（議題3）

- 追加セット: **大型車600**（v11core同型・車を大型化=至近/選別のE系評価を評価側に常設）
  / **長距離接近300**（100〜300mスポーン=E1b先行・「聞こえる前」の検知距離を測る）
  / キックボード300（採用時）
- 悪天候（風）は**合成では作らない**（風の物理モデル化は自作自演リスク。9月実録の
  実風データで評価する——実録計画に風収録は登録済み）
- 員数台帳を最初から作る（3,342会計の教訓）: plan生成時に台帳mdを自動出力

## 6. 事前登録ゲート（学習前に数値確定、現時点は枠のみ）

- 検品系: 背景形状±3dB / EVトーン卓越 / dB(A)較正±0.5dB / D2ビット一致 / 本人試聴A/B
- 学習系（v12-valで測る。v11とはデータが違うので直接比較しない）:
  - 基準線: **run3モデルをv12-valで採点した値**を先に取る（土俵を揃えた比較相手）
  - v12学習モデルが基準線を上回ること＋N2(EV)検出率の改善幅（数値は基準線取得後に
    宣言してから学習）
- 通知層: 重大無音化・安全抑制のv3.2指標で非劣化

## 7. 順序とマイルストーン

1. V12-0 サーバー移植＋ビット一致（本日）
2. V12-1 音源実装＋検品（背景→EV→大型車恒久化→[キックボード]の順）
3. V12-2 シチュエーション（車列・停止発進）
4. plan拡張（評価追加・台帳）→ 全量生成（サーバー）→ inspect
5. run3のv12-val基準線 → ゲート数値宣言 → 学習 → 評価 → 通知層
6. その後に物理ablation本番（v12土台の上で4条件）

## 追記: 列車の拡張音源規約（2026-08-05 05:55、本人指摘への対応）

本人指摘「列車は車両が長い＝通過後すぐ音が小さくなるのはおかしい」は点音源近似の
本質的な限界の指摘で正しい。v12の列車は以下の規約で実装する:
- **音響: 車両毎の複数点音源**（1編成=4〜8両を車両長20m間隔で同一軌道に配置、
  位相結合=各車両が同一ジョイントを車両長/速度ずつ遅れて踏む）
  → 通過中のプラトー（鳴り続け）と実物のリズム濃淡が物理的に再現される
- **ラベル: 単一トラック**（最近接車両の方向を代表値とする。ADPITの同一クラス
  最大3トラック制約と通知意味論=「列車が来ている」に整合。規約として明記）
- ガタンゴトンは軸位置の実幾何（台車中心=車端から3m・軸距2.1m・定尺25m）から
  発生時刻を導出（均等割りを廃止）
- 試聴・検品用に make_train_composite（編成合成）を用意。物理プレビューは
  本人方針により作らない（ドライ素材の耳ゲートのみ）

## 追記2: 耳ゲート全完了と統合チェックリスト（2026-08-05 15:15）

耳ゲート結果（本人判定）: 背景◯ / EV◯ / 大型車◯ / 列車=条件付き合格（シーンで識別可、
音色差は限界記録・実録後にフィット再較正可能な構造） / キックボード◯（シーン版11番）/
警笛=音圧省令値のみ要追補。**素材はすべて出荷可能状態**。

### 統合実装チェックリスト（step11_v12_render.py ほか）

1. **クラス辞書の分離（地雷）**: サーバーのdatasets/cls_indices_train.tsvは全データセット
   共有＝7クラス化すると既存v11系推論が壊れる。**v12は独立ルート datasets_v12/ を新設**
   （専用cls_indices 7クラス＋v12データセット）し、configの paths.dataset_dir で切替
2. step11_v12_render.py（チェーン方式、v11レンダラ継承・物理無変更）:
   - 背景: diffuse_foa_noise → noise_v12.diffuse_foa_urban_noise に差替
   - 車: audio_seed決定論抽選で EV15%（make_car_ev）/ 大型30%（heavyデルタ加算）/
     残りは現行make_car_v9
   - 列車: 新シナリオ（第4種踏切）= 車両毎の複数点音源（4〜8両・20m間隔・位相結合）、
     クラス=crossing（踏切・列車に意味拡張）、ラベル=最近接車両の単一トラック、
     警笛50%（長緩一声 or 短急数声、ランダム時刻・先頭車から）
   - キックボード: 新クラス7（特定小型原付: 車道1.7〜5.6m/s・歩道1.7m/s以下）
3. plan拡張（step10_v12_plan.py）: 既存7,200行は完全据置＋追加行
   （キックボード学習+900/val+225、列車学習+600/val+150目安）＋評価専用
   （大型車600・長距離300・キックボード300・第4種踏切150）＋員数台帳md自動出力
4. 検品: D2ビット一致（v12自身の決定論）・可聴性/dB(A)検品はstep11既存のinspect流用・
   新クラスのGT整合spot check
5. 学習系: run3レシピ流用＋クラス7対応（ヘッド初期化はSDE初期化と同じ流儀で6クラス分
   コピー+新クラス行ゼロ初期化が候補）。基準線=run3をv12-val6クラス部分で採点
6. 警笛音圧の省令値追補（法令DB原本）→ planの警笛レベル較正に反映

## 追記3: バイククラス採用（2026-08-05 15:30、本人GO）

- **クラス8=バイク（自動二輪・原付）を追加行方式で採用**。「新しい静音脅威
  （EV・キックボード）と従来の爆音脅威（バイク）の両端」でクラス構成を日本の
  歩道・路地の実勢に近づける
- 法規根拠: 加速走行騒音規制（7.5m測定）原付79dB(A)/軽・小型二輪82dB(A)
  （平成22年規制、JMCA規制値表・環境省中環審資料）→ planレベル帯は
  実勢70〜82dB(A)@7.5m（規制上限をキャップ）。速度は原付一種=法定30km/h、
  二輪市街地30〜60km/h。近接排気騒音84〜94dB(A)は参考（測定条件が別）
- 音源 motorcycle.py: 排気パルス列（発火周波数=rpm/120、原付50-75Hz/二輪35-55Hz）
  ＋マフラー共鳴整形＋発火同期ラスプ＋スロットル変動±12%。車との識別点=
  パルス性倍音とAM（車はなめらかノコギリ+タイヤ帯）
- 追加行規模（キックボードと同枠）: 学習+900・val+225・評価+300目安
- ベル無し自転車=限界として明記し実録でSNR検証 / 人の声警告・防災警報=第2段階表へ
  （クラス拡張の棚卸し結果、本人裁定 2026-08-05 15:24）

## 追記4: 耳ゲート最終確定（2026-08-05 15:40、本人裁定）

全音源の音色イテレーションを打ち切り、以下の優先順位で v12 の音源品質方針を確定:
1. **日本の規格・法規の数値遵守が最優先**（レベルdB・速度・吹鳴パターン・法定値。
   これらは全音源で規格帯較正済みであり不可侵）
2. クラス識別可能性（シーンとして「何の音か分かる」= 全音源クリア済み）
3. 音色忠実度はベストエフォート（実録参照フィットまで実施済み。以降の較正は
   9月実録の実測データで行う——取り直し不要のパラメータ生成器構造）

最終台帳: 背景◯ / EV◯ / 大型車◯ / 列車=条件付 / キックボード=条件付 /
バイク=条件付（実録フィット版）/ 警笛=音色実測フィット済・音圧省令値のみ要追補。
**全素材出荷確定。統合後半戦（plan拡張・8クラス・全量生成）へ移行**

## 追記5: v12学習の事前登録ゲート（2026-08-06 10:15宣言、v12モデルの結果を見る前に固定）

基準線（run3をv12-val coreで採点、正= out/v12_baseline_score/）:
6クラスrecallは維持（車98.1%）だが**EV車80.2%（通常99.3%）**・車方向5.10°・
至近距離中央0.64m・重大tier再現57.3%と、-6dBヘッドルーム＋新背景＋EV実物化で
run3は大きく劣化。v12学習モデル（v12-val、以下のゲート）:

- **G1（6クラス回復）**: 車の可聴検出率 ≥99% かつ 車方向誤差中央 ≤3.0°
- **G2（EV解放・主）**: 単独EVクリップの車recall ≥95%（基準線80.2%）
- **G3（新クラス成立）**: バイク ≥90% / 踏切・列車 ≥99% / キックボード ≥60%
  （キックの可聴26%設計=静音脅威を踏まえた床）
- **G4（距離回復）**: 至近≤5m中央 ≤0.30m（基準線0.64m）かつ 重大tier再現 ≥68%
- **G5（総合）**: val macro SELD ≤ 0.085（8クラス化・データ難化込みの床。参考v11=0.070）
- 解釈宣言: G2/G3が成立すれば「実物化＋新クラスは学習で獲得可能」。不成立項目は
  そのまま報告し、音源・配分の再設計判断に使う（事後の基準緩和はしない）
- 手続きメモ: 宣言時点で学習job266は走行中だが、v12モデルのいかなる評価数値も
  未取得（レシピはrun3同一で事前固定済み）。宣言→結果の順序は保たれている

## 追記6: v12学習の最終判定（2026-08-06 11:30、正= out/v12_model_score/）

### インシデント自己申告（fold3誤推論）
sedクローン元（v11学習用data yaml）のtest分割がfold3だったため、初回推論が
**封印対象のfold3（本番テスト）**に走り、集計値を一度閲覧してしまった。レシピ・ゲートは
事前固定済みでチューニングには未使用だが、fold3の数値は**封印**（意思決定に不使用）とし、
本判定はfold2（正規val 1,800本）で実施。再発防止=valinfer系yamlを正としてクローン。

### 事前登録ゲート判定（fold2・基準線=run3 on v12-val）

| ゲート | 基準 | 実測 | 判定 |
|---|---|---|---|
| G1 6クラス回復 | 車≥99% かつ 方向≤3.0° | **99.61% / 2.00°** | ✅ |
| G2 EV解放（主） | 単独EV recall ≥95%（基準線80.2%） | **99.38%**（+19.2pt） | ✅ |
| G3 新クラス | バイク≥90/列車≥99/キック≥60 | **99.80 / 100.0 / 97.61%**（方向1.0°/3.1°/1.0°） | ✅ |
| G4 距離回復 | 至近中央≤0.30m かつ 重大tier≥68% | 至近**0.26m**✓ / 重大**66.5%**✗ | ❌（後半のみ未達） |
| G5 総合 | val macro SELD ≤0.085 | **0.086** | ❌（0.001僅差） |

- 補助: 大型車recall 99.50%・距離帯別は全帯でv11水準回復（0-1.5m中央0.19m）。
  踏切クラスのdist MAE 15.3mは列車の長距離GT（〜278m）混入による定義通りの拡大
  （中央2.78mは健全）
- 結論: **主目的（実物化EVの解放・新3クラスの成立）は学習で達成**。G4後半（重大tier
  66.5 vs 68）とG5（0.086 vs 0.085）は僅差未達として記録（事後緩和しない）。
  改善レバー候補=W_DIST・エポック・キック/バイクの距離ラベル追加学習は
  ablation本番と別枠で今後判断

## 追記7（2026-08-06 23:30）: G4/G5再学習とw3の本番候補採点

- レバー実行（第8回監査対応バッチ内）: (a)W_DIST=3.0新規=**w3**（ep099）
  (b)run1 ep094から継続100ep=cont（ep089）。いずれもlog距離・他はrun1同一
- ゲート再判定（基準は宣言時のまま・緩和なし）:
  w3=**G1✓G2✓(EV99.27%)G3✓(キック96.55/バイク99.76/踏切列車100)G4✓(重大tier69.1%・
  至近中央0.25m=初達成)** G5✗(0.087)。cont=G4✗(67.9%)G5✗(0.086)。
  **G5(≤0.085)は全レバー未達のまま記録**
- w3の通知・eval再採点:
  - core通知v3.2: 強到達**71.0%**/抑制91.6%/誤強6（run1: 67.9%/90.7%/4）
  - 新クラス通知: **キック重大の強到達93.1%**（run1 79.5%）・バイク誤発火0
  - eval拡充: heavy 99.25%(≒run1)・far サイレン208m不変・車18/31検知(+4)・
    **kick苦手(騒音58-65dB) 85.8%（run1 91.2%から-5.4pt=距離重視の代償）**・bike不変
- 判断: **役割③（距離・通知）が主戦場のためw3を本番候補に採用推奨**。
  キック高騒音SEDの後退は既知トレードオフとして監視（改善はキック距離ラベル
  追加学習など次期レバー）。run1は比較基準として凍結保存
