# 役割③（至近車のリアルタイム区別）: 距離推定(SDE)実装 設計メモ

2026-08-03深夜。段階①完了、段階②の設計確定版。正の数値はrun2（server ep094）。

## 確定済みの事実（コードで裏取り済み）

- pin版PSELDNets(8092a14)に距離推定は**無い**（distヒットは全て無関係）
- 既存metadataは5列 `[frame, class, track, az, el]`＝**距離列なし**（引き継ぎ書の「5列目=距離」は誤りだった）
- ただしローダ`load_output_format_file`は**6列/7列を既に受理**（距離は読み捨て）
- ADPITラベルは `[frames, 6, 4(=act+XYZ), classes]`（preproc/preprocess.py `_extract_adpit_label`）
- 出力ヘッド（models/multi_accdoa.py HTSAT）: `tscam_conv out=classes*3*3`
- ADPIT損失（loss/multi_accdoa.py）: 13通り置換のmin選択、`target[:, :, i, 0:1, :]*target[:, :, i, 1:, :]`

## 段階①（完了・コミットf8bb6b8）

- `scripts/step19_dist_labels.py` → `out/dataset_outdoor_siren_v11/metadata_dist/` に6列7,200本
- 検証: 全986,199行でaz/el再計算が既存と整数一致・空589本=設計値一致
- 実装注意: masksのSNRは2桁丸め保存→可聴ゲート再現は不可。**既存行を正**とし距離だけ付与する方式

## 段階②の設計（確定）

方針: pin版を直接書き換えず、**新規ファイル追加＋新config**で実現（provenance維持）。

1. **ラベル**: `_extract_adpit_label` の距離対応版 → `[frames, 6, 5(=act+XYZ+dist), classes]`
   - ローダ改: 6列時にdistを保持（`[class, az, el, dist]`→cartesian変換後 `[class,x,y,z,dist]`）
   - 距離の正規化: **d/10**（0.6〜44m → 0.06〜4.4。線形・可逆・1行で変更可能に）
   - 出力先: `_label/adpit_dist/...` （既存adpitラベルは不変更）
2. **モデル**: `models/multi_accdoa_sde.py` — HTSAT継承、`tscam_conv out=classes*3*4`
   - **ウォームスタート**: run2 ckptの旧ヘッド重み(3軸ぶん)を新ヘッドの対応行にコピー、
     **距離行はゼロ初期化** → 初期状態は「run2と同一挙動＋距離出力0」
     ＝計画の「距離0固定で既存と同一」検証ゲートが初期値で自動成立
   - 変換スクリプト: `scripts/_make_sde_init_ckpt.py`（ローカルでckpt変換→サーバへ）
3. **損失**: `loss/multi_accdoa_sde.py`
   - 置換(13通り)の選択は**xyzのみ**で行う（既存と同一の選択規準→SELD挙動を乱さない）
   - 距離項: 選択された置換に対し、活動マスク付きMSE × 重みW_DIST（既定5.0、cfg可変）
4. **推論**: model_module の出力書き出しに距離列を追加（pred CSV 6列目=推定距離[m]）
5. **config**: 新experiment yaml（dataset=outdoor_siren_v11 / モデル=SDE版 / ラベルパス=adpit_dist / init ckpt=変換済みrun2）

## 段階③（サーバ学習）の手順

1. metadata_dist をサーバへ（zip小・数十MB）
2. サーバでADPIT距離ラベル抽出（音声特徴は既存流用＝再抽出不要のはず。要確認:
   featureとlabelのh5が分離しているか）
3. 学習: run2構成流用＋SDE版・warm start ckpt・短め(50ep程度)から
4. 検証ゲート: val SELD がrun2(0.075)から劣化しないこと／距離MAE・3段仕分け一致率の実測

## 段階④（通知層v3）

- 新ファイル step12_notify_v3（v9は不変更）: 車発火時の推定距離で
  ≤1.5m=役割③(強) / ≤3.0m=役割②(中) / >3.2m=抑制
- 主指標: GT tierとの一致率、safe過剰通知の削減、危険層通知・リードの維持
- 第7回監査の要件を継承: 全数分母の≥2.5s成功率・距離層別・単一車主解析・因果性

## 実装詳細（コード精読で確定、2026-08-03深夜）

- **現役のADPIT抽出は `extract_adpit_label`（アンダースコアなし）**で、h5は
  `{fn}/adpit/{se,azi,ele}` 各[frames,6,C]（bool/int16/int8）。行列版
  `_extract_adpit_label` は未使用系。学習時は data/data.py `DatasetMultiACCDOA.__getitem__`
  がse/azi/eleからxyzを再構成 → SDEは**getitemラッパ**でdist軸(5軸目、d×0.1×act)を追加
- **チャンネルレイアウト**: `(track*3+axis)*C + class`（get_multi_accdoa_labelsの
  スライスで確認）→ SDEは `(track*4+axis)*C + class`。ckpt変換の対応表もこれ
- **モデル解決**: models/model_module.py の `ModelMoodule` dict →
  `vars(models.multi_accdoa)[backbone]` → `models.multi_accdoa.HTSAT` 属性差し替えでOK
- **トラック統合**(threshold_unify=15°)はマージ時に平均 → SDE版はdistも平均
- ローダ`load_output_format_file`は6列を既に受理（distは読み捨て→パッチで保持）
- 空ラベル対応は `{99:[]}`（=100フレーム・イベント0）をローダパッチに内蔵

## 実装物（server_sde/、コミット済み）

`sde_patch.py`（全パッチ本体）/ `_train_sde.py` / `_preproc_sde.py` /
`_make_sde_init_ckpt.py`（ウォームスタート変換）/ `test_sde_units.py`（等価性テスト
T1損失xyz等価・T2抽出等価・T3ヘッド拡張）

## サーバ展開の手順（次にやること）

1. `server_sde/*` を `is-server:~/PSELDNets/` へ、`metadata_dist/`(zip約35MB)を
   `datasets/outdoor_siren_v11/metadata_dist/` へ
2. **確認2点**: (a) run2実験yamlのaugment設定（rotate/ACS等が有効ならパッチ追加要）
   (b) `models/components/model_module.py` L215-227のreshapeが4Cブロックを素通しできるか
   （明示の3*nbがあれば要パッチ）＋ run2のckpt初期化機構（どのcfgキーか）
3. `python test_sde_units.py`（T1/T2/T3 全PASSが学習開始の条件）
4. `python _preproc_sde.py mode=extract_data dataset=outdoor_siren_v11 dataset_type=train`
5. `python _make_sde_init_ckpt.py <run2 epoch_094> <init_sde.ckpt>`
6. `_train_sde.py` で学習（srun a100_1g、run2レシピ流用）
   検証ゲート: val SELDがrun2(0.075)から劣化しない／距離MAE・3段仕分け一致率

## 実行記録（2026-08-03 深夜、サーバ展開まで完了）

- リポジトリ実体= `~/research/PSELDNet/PSELDNets`（~/PSELDNetsはほぼ空だった）。
  dataset_type=**dev**（h5は label/*/dev/）。**augmentはtype:[]=全部未使用**→追加パッチ不要。
  model_module.pyのreshapeは`-1`で4Cブロック素通し→追加パッチ不要
- 既存load_ckptsはtscam_convを必ずスキップ(accdoa.py L198)→ HTSAT_SDEが
  環境変数`SDE_INIT_CKPT`からヘッドを自前ロードする方式に変更済み
- **等価性テスト全PASS**: T1損失xyz等価 diff=0.00 / T2抽出等価200本 / T3ヘッド拡張
- adpit_dist h5生成済み(7,200本→_hdf5/label/adpit_dist/dev/)。ckpt変換済み
  (net._orig_mod.tscam_conv 54→72ch、距離行0、→~/PSELDNets_logs/sde_init_from_run2ep94.ckpt)
- **学習ジョブ192投入・実行中**(pro_6000, experiment_name=outdoor_siren_v11_sde_run1、
  ログ=~/research/PSELDNet/PSELDNets/v11sde1-192.out)。run2実績では100epが約24分
- サーバdataset は7,199本時代（mix0181除外）だが学習はfold1+2のみで無影響
- 学習後の残り: 距離MAE/3段仕分け一致率の採点スクリプト、val SELD劣化チェック
  (ゲート: run2=0.075から悪化しない)、推論のdist付きCSV出力→step12_notify_v3

## 実録との接続（本人合意事項）

- 実録評価データのGT距離には**動画の並行記録が必要**（2026-08-03本人指摘）
  → 実録収録計画v2にスマホ動画記録を追記すること（装置の部品ではなく採点用参照）
