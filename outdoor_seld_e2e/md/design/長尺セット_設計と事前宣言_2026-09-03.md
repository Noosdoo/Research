# 長尺セット v1（60 秒 × 400 本）— 設計と事前宣言（下書き・2026-09-03 18:30）

本人判断（2026-09-03 13:24）「③ 流れモードを v2 の規則に入れる＝A」を受けて新設する評価セット。
v4.3 と同じ作法: **先に宣言 → fold40 で掃引 → fold41 で確認**。この文書は「セットの設計」と「測り方」の宣言で、
流れモード自体のパラメータ候補は別文書（通知 v4.5 の事前宣言）で掃引前に固定する。

## 1. 目的

幹線の歩道を歩くと車が次々通り、「注意（中）」が続けて鳴る（本人「鳴らしすぎ」）。10 秒のクリップでは「続けて鳴る」状況が
入らないので、60 秒の場面で **(a) 注意が 1 分に何回鳴るか**、**(b) 流れモードでどれだけ減るか**、**(c) 減らした注意のうち
本当に必要だったもの（その車が後で至近になる）がどれだけあるか＝害** を測る。

## 2. セットの構成（step10_long_plan.py・seed × 20260940 で決定論的）

| 項目 | 値 | 根拠 |
| --- | --- | --- |
| 本数・長さ | fold40（選定用）200 本＋fold41（検証用）200 本、各 60 秒、晴れ・雨なし | 害を ±2pt で見るのに 1,000 台規模 → 200 本×約 5 台 |
| 交通量（幹線） | low 2〜4 / mid 4〜8 / high 8〜12 台/分 を等分。両車線合計、ポアソン到着 | 本人指摘「3〜6 台/分は幹線としては少なめ、密度別に見たい」 |
| 場面 | 幹線の歩道を歩く 70%、幹線で静止（信号待ち・バス停）15%、住宅街（0.5〜1 台/分）15% | 流れモードの対象は幹線の歩道。他は「何も起きない」確認 |
| 幾何 | 左側通行。歩行者は左の歩道。近い車線（横 3.0〜4.5 m）は後ろから、対向車線（横 6.5〜8.5 m）は前から。速度 30〜50 km/h | 実際の幹線の歩道 |
| 至近を含む本 | 幹線の 25%（半分は左折巻き込み＝後ろから来て目の前で左折、半分は至近の徐行通過 0.8〜1.2 m・10〜20 km/h）、住宅街の 50% | 害の測定に「後で至近になる車」が要る |
| 警告音 | 後ろから自転車ベル 15%、クラクション 5% | 警告経路が流れモードの影響を受けないことの確認 |
| 音量・暗騒音 | 学習データと同じ規約（_draw_level・NOISE_DBA。幹線 52〜65 dB(A)、住宅街 40〜50） | 分布を学習データから離さない |
| 音の生成 | デモ v3 ツールの幾何（直進・左折・徐行）＋ m9 の描画（地面反射・歩行マイク） | 既存の合成器そのもの |

ラベルの可聴ゲート: 学習データは実信号のマスクで決めるが、ここでは「1 m 相当レベル − 20 log10(距離) ≥ 暗騒音」の
距離近似（意味は AUDIBLE_SNR_DB=0 と同じ）で決め、80 m より遠い車はラベルにしない。評価専用なので近似で足りる。
track 番号は同時に鳴る同クラスに別番号（貪欲法）。

## 3. 推論

因果推論（過去 10 秒の窓を 0.1 秒ずつ進める）を 60 秒に対応させる: `server_sde/_causal_infer_long.py`（10 秒の右詰め窓を
600 フレーム分）。モデルは採用の ft2 e099。400 本 × 600 フレーム ＝ 24 万フレーム（A100 で約 10 分）。

## 4. 測り方（先に固定）

通知は v4.3 ＋ 警告音 hold を 60 秒にそのまま当てる（規則は 10 秒に依存しない）。帰属は方位帰属（±0.5 s・≤30°）。

| 指標 | 定義 |
| --- | --- |
| 注意の回数/分 | 中の発火数 ÷ 1 分（幹線の歩道・密度別） |
| 強の回数/分 | 同上・強 |
| 至近の強到達 | 至近（≤1.5 m）になった車に、至近になる前に強が出た率（v4.3 と同じ定義） |
| **害** | 流れモードが抑えた「中」のうち、その車（方位帰属）が後で至近（≤1.5 m）になった割合 |
| 警告音 | ベル・クラクションの警告が流れモードで変わらないこと（変化 0 件） |

判断基準（掃引前に固定）: fold40 で **注意/分 −30% 以上 かつ 害 ≤ 2%**、かつ **至近の強到達 −1pt 以内** の構成を候補にし、
fold41 で同じ条件を満たせば採用。満たす構成が無ければ「流れモードは v2 に入れない」で確定（機器側の束ねだけ）。

## 5. 流れモードの候補（v4.5 事前宣言で確定する。ここは下書き）

- 同じ側（方位 ±45°）で「中」が K 回（K ∈ {2, 3}）W 秒（W ∈ {10, 20}）以内に続いたら、以後の「中」を抑える
- 解除: その側で強が出た／C 秒（C ∈ {10, 20}）中が無かった
- 強・警告音は抑えない

## 6. 状態（2026-09-03 18:30）

- 計画表・場面仕様・描画器を実装（`scripts/step10_long_plan.py`, `scripts/step11_long_render.py`）。試作 6 本（各層 2 本）を描画し
  `out/dataset_outdoor_long_v1/listen/` に試聴用ステレオ → **本人の試聴待ち**
- 未実装: 60 秒対応の因果推論（§3）、採点器（§4）、v4.5 の事前宣言（§5）
- 400 本の本生成はサーバ（pro_6000 で 1 本 30 秒前後 → 12 並列で 10 分）。試聴 OK 後に投入
