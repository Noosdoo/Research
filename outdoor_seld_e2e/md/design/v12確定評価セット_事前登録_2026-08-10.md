# v12確定評価セット（conf）事前登録（2026-08-10、生成前に凍結コミット）

目的: Sol第8回再監査の残条件①「事前固定した新規未使用セットで確定値を一発評価」の
消化。本文書のコミットが事前登録であり、生成・推論・採点はこの後に1回だけ行う。

## 構成（valの鏡像・新シード）

- テンプレート: fold2の全1,800行（core 1,200 = v11 core planのfold2行、
  ext 600 = v12 ext planのfold2行: kick225/bike225/train150）
- 設計フィールド（motion/scenario/danger_tier/car_side/n_car等）は**完全保存**し、
  変更は3点のみ: clip_id→fold20_room1_mix0001〜1800（テンプレート順）、
  split→fold20、seed→新系列
- シード: SEED_BASE=12_000_000_000 + idx*104729。core/ext/eval全planとの
  排他をplan生成時にassert
- fold20はいかなる学習・検証・過去評価にも未使用
- 生成器・物理・較正は本番と同一（step11_v12_render.generate_clip_v12）

## 凍結する評価対象（この後一切変更しない）

- モデル: v12 w3 = outdoor_siren_v12_sde_w3/checkpoints/epoch_099.ckpt（変更なし）
- 距離復号: SDE_DIST_MODE=log
- 通知規則: v3.3(連結±25°)とv3.4(連結±60°)の両方を採点し**両方報告**。
  主報告=v3.4（停止規則済みの現行凍結規則）。閾値T3/T2/SUPP/帰属±25°は不変
- 採点器: 本コミット時点のHEAD（manifest分母・episode網羅・8クラス表記）

## 報告する確定値（すべて、良否にかかわらずそのまま採録）

1. 8クラス可聴recall・方向誤差（_v12_score class_table、manifest分母）
2. 距離: 至近≤5m中央・重大tier再現（=G4の2指標）・8クラス距離表
3. core通知（fold20 mix0001-1200）: 強到達・抑制・誤強・拡張KPI（v3.3/v3.4）
4. 新クラス通知（v12b、v3.3/v3.4）: キック/バイクの強到達・誤発火
5. B2個票監査（残条件②）: 重大車で強未達の全トラックを
   A(トリガ未成立)/B1(最近傍負け)/B2(他車帰属)/B3(GT行なし)に分類し、
   B2は個票（他車の実距離・tier）で「正当（他所に実在の重大車）/スチール」を判定

## 開示事項

- 本コミット前に配管確認としてmix0001とmix1800の2本のみローカルでスモーク生成した
  （生成器は決定的のためサーバ生成と同一内容。推論・採点には未使用）
- B2個票コードの動作確認はfold3（既知データ）で実施した（コードパス検証のみ、
  出力はスクラッチ破棄。fold3のB2内訳: A101/B1 1/B2 19(正当11・中間8・スチール0)/B3 4）

## 停止規則

- 生成は1回・推論は1回・採点は1回。結果を見た後の再生成・再学習・規則変更・
  セット差替は行わない。数値が開発値より悪くてもそのまま確定値として報告する
- 本セットは以後「確定評価セット」として凍結（卒論の確定値もこのセット）

## 手順（サーバ1ジョブで一気通貫・途中介入なし）

生成(6シャード)→距離GT(全行検証)→ラベル抽出→w3推論→連結→core通知v3.3/v3.4
（実行ファイルsha256・git SHAをログに記録）→ローカルでclass/dist/v12b採点→
B2個票→確定値報告→Sol最終検証。
