# 確定評価v2 事前登録【下書き・未発効】— 2026-09-07 作成

⚠️ **これは下書き。発効していない。** 発効の手順（第10回監査 論点2）: 10 月末に `md/design/v2final_params.json` を fixed にし、
本書の ＿＿ を全部埋めて日付を入れ、コミットした時点で発効する。**埋める前に fold20 で v2 系のモデルを 1 回でも走らせたら不合格。**
（`server_sde/v2final_conf.sbatch` は params が fixed でないと走らず、1 回走ったら二度目を拒否する）

テンプレート = `確定評価v2_事前登録テンプレート_2026-08-30.md`。束の設計 = `確定評価v2_束の設計案_2026-09-03.md`。実行計画 = `確定評価v2_最終学習_実行計画_2026-09-07.md`。

---

# 確定評価v2 事前登録（＿＿＿＿年＿＿月＿＿日）

## 1. 何を評価するか（これで固定）

- **学習データ**: v2final（`scripts/step10_v2final_plan.py` ＋ `_run_v2final_gen.py`、つまみ = `md/design/v2final_params.json`）
  - params の status=fixed の日付: ＿＿＿＿ / sha256: ＿＿＿＿
  - 土台 = v17b レシピ（v13 の束 S1〜S4・歩行 D4=＿＿%・雨 D3=20%（D2 レベル ＿＿）・D10 至近低速 50%・D13 高さ 1.4〜2.1 m ×2・装着高さ入力）
  - 生成ジョブ: `v2final_gen.sbatch` job ＿＿＿＿ / assignment_v2final.csv sha256 ＿＿＿＿ / 18,000 本
- **基準学習**: `outdoor_siren_v2final_sde_w3`（w3 レシピ・100ep・init = `v12_init_from_run3ep84.ckpt` sha256 ＿＿＿＿）job ＿＿＿＿
- **因果 ft**: `outdoor_siren_v2final_causal_ft`（lr1e-4・150ep・StepLR90・KMIN40・REPEAT2・高さゆらぎ 0.10）job ＿＿＿＿
- **モデル（確定評価に使う ckpt）**: `outdoor_siren_v2final_causal_ft/checkpoints/epoch_＿＿＿.ckpt`（sha256 ＿＿＿＿）
  - 選び方（事前固定・実行計画 §4）: 開発 val（v12 fold2 1,800 本・1.5 m）を統一採点器で採点し、**至近帯（≤1.5 m）の距離誤差が最小**の ckpt。同率なら至近捕捉率、さらに同率なら後の epoch。fold20 は選定に使わない
  - 選定の記録: `out/audit_rescore/v2final_select.md`（＿＿）
- **推論**: 因果推論 `_causal_infer_v17.py`（未来参照なし・KMIN40・REPEAT2）。**装着高さ入力は表なし・既定 1.5 m**（fold20 は v1 と同じ 1.5 m 固定の合成データ）
- **距離の補正表**（束 §6.1 判断 1・規則の前段に置く）: v2 モデルで fold32 から作り直し、fold30 で確認してから固定。表の保存先 ＿＿＿＿ / sha256 ＿＿＿＿。
  作らない判断をした場合はその旨をここに書く（＿＿）
- **通知規則**: **v4.3**（`step12_notify_v43.py` Cfg43 = `out/notify_v43_sweep/winner.json` sha256 ＿＿＿＿）＋ 警告音の hold（アンカー方位固定）。⑤交通量モード・流れモード（v4.5）・車の hold（v4.6）は**入れない**（不採用で確定・2026-09-03）
- **評価セット**: 確定評価セット core 1,200 本（fold20・`outdoor_siren_v12_conf`・v1 と同一・不変）
- **採点器**: v1 と同一の `_v12_conf_notify_server.py`（`step12_notify_v33.py` 系・車ごとの方位帰属 ±25°・帰属不能は別計上）。車の発火は v4.3 の fires に置換、警告音は hold 方式。出力 = `out/v2final_conf_notify_v33` / `_v34`
  - 限界: 同一方位に 2 台が重なる場合は区別できない
  - 開発用の数値（統一採点器・帰属方式）は「開発用採点器」のラベル付きでしか引用しない
- **実施回数**: **1 回だけ**（`V2FINAL_CKPT=<上の ckpt> sbatch server_sde/v2final_conf.sbatch`）

## 2. 報告の拘束（選択的報告の封じ・第10回監査 論点2条件2）

**v1（至近到達 73.0%）と v2 は卒論・発表で必ず併記し、「現行システムの確定値」は v2 で固定する。v2 が v1 より悪化しても差し替え・非掲載にしない。**
v2 の束（D1〜D13・v17b レシピ）は本書のコミットで固定し、確定評価の後に束を変えた版の数値は「開発値」としてしか出さない。

## 3. バージョンの打ち止め（同条件3）

確定評価は **v2 で打ち止め**（卒論まで）。以後のシステム変更は確定評価を伴わない開発値として明示的に区別する。v3 は作らない。

## 4. 既知の限界（v2 登録時に転記して残す）

- fold20 は v1 で集計レベルの結果が既知（軽い汚染）。クリップ単位の調整は行っていない。最終の外部チェックは実録評価（210 本・A′ で最終評価専用）
- v4.2 のしきい値はチューニング専用 val（fold31）で選定・fold30 で検証、v4.3 の routeC 3 点は fold32 で選定・fold30 で検証（2 回目の使用）。fold20・実録は未使用
- **fold20 はマイク 1.5 m 固定**なので、v17b で入れた「装着高さの入力」の効果は fold20 では測れない（高さの効果は開発 val の帯別と実録で示す）
- v2 のデータには 10 月の実録で決めたつまみ（D2 雨レベル・D4 歩行比率・D7 風）が入る。調整に使ったのは負例露出・校正・予備試験・210 本の外の統制試行だけ（A′）。事象テイクは見ていない
- 実録の採点（step20 v3・記録紙基準のラップ）は fold20 の採点器とは別物で、数値を直接比べない

## 5. 併記する評価枠（確定値ではない・束 §3）

- 開発 val（v12 fold2 1,800 本）: 統一採点器（v4.3＋hold・方位帰属・manifest 固定・1 対 1・全 GT 分母）。高さの帯別（1.4〜1.6 / 1.6〜1.85 / 1.85〜2.1 m）
- 長尺セット v1（60 秒 × 400 本・fold40/41）: 安全な車への誤った中・強の率、同じ車への再発火率（`_long_score.py`）
- 機器層の再生評価（`_device_replay_eval.py`・5 振動子）: 至近で振幅 ≥0.8、誤った向き、反対側の振動子
- 実録（10〜11 月・210 本＋負例 100 分）: step20 v3。確定評価とは別に報告

## 6. 実施記録（実施後に記入）

- 実施日時: ＿＿＿＿ / ジョブ ID: ＿＿＿＿ / git HEAD: ＿＿＿＿ / sha256 記録: `v2final_conf_sha256_job＿＿.txt` / 結果の保存先: ＿＿＿＿
