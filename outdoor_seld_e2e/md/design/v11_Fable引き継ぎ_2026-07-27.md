# Fable 引き継ぎメモ（2026-07-27、Opus 5セッションから）

> このファイルは「学習理解＋データセット監査＋v11設計」を行った会話（Opus 5）を
> Fable セッションが引き継ぐための道標。**まずこれを読み、次に §末尾の関連ファイルを開く。**
> 元会話の生ログ: `~/.claude/projects/c--Users-satos-research/c84bd1dc-…jsonl`
> （Claude Code で `/resume` → session-id `c84bd1dc-dcc0-49c9-90da-bfcc938dd30c` を選べば全履歴を復元可能）

## 1. この会話で何をしたか
1. **①〜⑮の通し解説**（本人の理解のため、物理→データ→モデル→学習→評価→実験→監査を平易に）。
   本人は自分の言葉でメモを完成（①物理式〜⑮監査）。未記入=「v9.2実験」「SELDメトリクス」。
2. **データセット十分性監査**（実データ実測）→ 穴4つを確定。
3. **v11拡張設計**を確定（実装はFableに委譲）→ `md/design/v11データセット拡張_設計書_2026-07-27.md`。

## 2. 本人が下した決定（v11の前提。設計書§8にも記載）
- **学習分布=自然頻度寄せ＋重要セルのフロア**（シーン種別 住宅40/中40/幹線20で車台数を自然決定。
  ただし 警告のみ・車なし≥400 / 純静穏≥300 / 複数車≥1500合計 を絶対数で保証）。
  警告音は検出対象ゆえ意図的にオーバーサンプル＝卒論に明記する方針。
- **新種シナリオ N1〜N7 全部作る**（見通し不良右左折/静音EV/駐車場複数バック/緊急車ドップラー/
  繁華街多重/後方追い越し/停車車発進。雨天強風は物理未対応で保留）。
- **実録アンカーは後回し・合成拡張を先行**。
- **規模**: core 3600→**7200本(20h=DCASE公式合成相当)**、split4:1:1=4800/1200/1200。

## 3. この会話でしか出ていない重要な診断・気づき（memoryや設計書に散在するが要点再掲）
- **threshold_unify=15° の切り分け診断（ローカル実測、2026-07-27）**: v10a で真に車2台のフレーム
  4,765を分析 → 両方報告=真の方位差 中央118.5°(<15°は4.8%) / 片方落ち=中央9.0°(<15°が65.4%)。
  **近接2台の取りこぼしが62.5%頭打ちの主因候補**。ただし片方落ちの35%は>15°=統合では説明不可。
  → 設計書§4.5「デコーダ閾値掃引」。**pin済submoduleは編集せず引数化**。統合前トラック別出力の
  保存に**再推論が要る**（予測CSVは統合済みのためローカルだけでは掃引不可）。
- **目的の正しい括り（本人が訂正させた）**: 6クラス全部が「健聴者なら聞こえて避けられるのに
  難聴者には聞こえない音」で1カテゴリ。車の走行音も別種でなく"最も拾いにくいメンバー"。
  「警告音5種/n_warnings」はシーン生成上のラベルで危険分類ではない。
- **本人の鋭い指摘2件が的中**:「全クリップ車ありはおかしい」「評価本数が少ない」→v11の穴埋めの核。
- **リアルタイムは未実装**（重要な限界。本人メモに未反映）: あるのは因果推論(オフライン模擬)＋
  速度測定(T4上11.3ms=25窓バッチのスループット)だけ。実機システムなし。HTSATは140MB/約3500万param。
  今の因果推論実装は0.1秒ごとに10秒窓を再計算＝約100倍冗長。卒論は「リアルタイムの可能性を示した/
  実装は今後」と2段で書く。

## 4. Fable が次にやること（設計書§7の着手順）
1. `step10_v11_plan.py`（n_carグリッド＋警告のみ＋複数車統合、決定論md5＋全数検算precheck）
2. `step11_v11_render.py`（v10レンダラをimport＋n_car/警告のみ経路）→ precheck → 分割生成
3. 評価セット増量（幻覚・safe過剰通知→600、S1/複数車/S5→200）＋新種N1-N7
4. 学習（Colab、EXP=outdoor_siren_v11_run1、Drive永続化＆自動resume）
5. fold3最終テスト1回（cell_fold3_eval系が準備済み）
6. デコーダ閾値掃引（§4.5、再推論セルに統合前トラック別出力の保存を追加）
7. 実録アンカーは器材調達後（§4）

## 5. 注意・規約（この会話で再確認した不変量）
- **凍結ポリシー**: 評価/デコーダ層は自由、学習系（データ・音源・ラベル・較正）を変えるなら全量再生成。
- **新バージョンは新規ファイル**（v11は別folder、v10系は据え置き）。
- **ipynb/設定は差分提示**（直接編集しない）。**Colabは Drive永続化＋固定EXP名＋自動resume**。
- 生成・採点python=`dynamic-sound/.venv`、学習=`PSELDNets/.venv`。
- 数値の絶対値比較はしない（土俵が易しい）→版間・条件別・誤り解剖の相対比較で主張。

## 6. 関連ファイル（開く順）
1. `md/design/v11データセット拡張_設計書_2026-07-27.md` … v11の正（規模・構成・新種・§4.5掃引・着手順）
2. `~/.claude/.../memory/project_outdoor_seld_e2e.md` 末尾 … 監査＋v11決定の記録（自動読込）
3. `md/explain/データ構成と実験組み合わせ一覧_2026-07-23.md` … 現行v10土俵の全構成
4. `md/explain/通し解説_2026-07-19.md` … 版の物語（v1→v9.2）
5. 本人の手書き学習メモ（①〜⑮、本人が別途保有。ゼミ説明の骨子）

## 7. 未確定（本人判断待ち・設計書§9）
- 実録の器材（FOAマイク機種・騒音計）と収録場所・時期。
- シーン種別割合（住宅40/中40/幹線20）の微調整は生成後の分布検品で確認。
