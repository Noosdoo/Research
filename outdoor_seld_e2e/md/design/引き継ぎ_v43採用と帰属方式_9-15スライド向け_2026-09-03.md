# 引き継ぎ — 通知層 v4.3 採用と採点の帰属方式（9/15スライド作成チャット向け）2026-09-03 09:50

`引き継ぎ_通知層v4.3_検討依頼_2026-09-03.md` の依頼への回答。**以下をそのまま貼る。**

```
通知層の現行が変わりました。スライドの数値はこれに合わせてください。

## 1. 結論（3行）
1. 「安全×すり抜け型の強 61→114」の大半は採点の帰属の artifact（別の近い車への正しい強が、窓内の安全な車にも
   数えられていた）。方位で紐づけ直すと v4.1 5件 → v4.2 24件（fold31）。
2. 残る本物の誤強は routeC の方位分解能不足（整数度の方位を0.5秒で微分）が主因 → routeC だけ
   距離12m・0.07rad/s・1.3秒窓に変更 = **v4.3**。
3. v4.3: fold30 で誤強 35→17件（−51%）、強到達 89.3→89.3%、安全抑制 76.9→78.6%。採用条件クリア →
   **本人決定（2026-09-03 09:36）: 現行は常にすべて v4.3**。

## 2. 正本（数値はここから）
- md/results/通知v4.3_採用結果まとめ_2026-09-03.md  ← 通知層の数値の正本（v4.2 は「v4.2（旧）」ラベル別枠のみ）
- md/design/通知v4.3_検討の事前宣言_2026-09-03.md   ← 作法（宣言→診断→fold32選定→fold30検証）
- out/notify_v43_sweep/fold30_table/q2_table_attrib.md ← 型別表（窓帰属・方位帰属の併記）

## 3. 数値の書き方の新ルール（本人決定「B」・第11回監査 論点6）
- 採点の帰属方式を**必ずラベルで付ける**: 「窓帰属」= 従来の採点器、「方位帰属」= 警告を方位で車に紐づける新方式。
  同じ規則でも 強到達 89.3%（窓）/ 80.4%（方位）、安全抑制 78.6%（窓）/ 91.0%（方位）と数値が違う。
- 中間発表で使った確定値（至近到達 73.0% 等・fold20・v1）は**もともと車ごとの方位帰属**（採点器 v33）なので
  健全。そのまま使ってよい。差し替えない。
- 「窓帰属で高め」なのは開発用 val の到達率（fold30 の 89.3% 等）だけ。これを引くときは
  「窓帰属（開発用）」のラベルを付け、確定値 73.0% と同じ行・同じ文に並べない。
- スライドで v4.3 の数値を出すときは開発値（fold30/fold32）である旨と帰属方式を添える。
  例:「強到達 89.3%（fold30・窓帰属・開発値）」

## 4. 触らないもの
PROGRESS.md / md/design/再学習④_* / v13設計書 / scripts/step12_notify_v43.py / scripts/_notify_v43_* /
out/notify_v43_* / 計算サーバ。コミットは自分のファイルだけ指定。
```
