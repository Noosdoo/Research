# ヘルメット収録ノート レビュー（2026-09-06）への対応 — 2026-09-07 00:10

対象: [レビュー報告書](ヘルメット収録ノート_レビュー_2026-09-06.md)（H01〜H06）。ノート本体・変換器・切り出し器を直し、
監査の再現手順をそのまま自動試験にした。スマホ実機（表示・共有・再起動後の保存）は本人が未検証。

## 対応表

| 指摘 | 直したこと | 確かめ方 |
| --- | --- | --- |
| **H01** 校正 id 不一致・点検原本で停止 | 変換器 step19d の校正 id を **校正原本の stem**（例 `ZOOM0001`）にし、step19 `--calib raw/ZOOM0001.wav` の既定 id と一致させた。session.csv の `点検原本`・`点検方式` を注釈に引き継ぎ（`check_file`・`点検方式` 列）、step19b は点検原本を**明示的に**切り出し対象から外す（未対応検査はそのまま生きている）。さらに step19d が `<ann>_sessions.csv` を隣に書き、事象 0 行のセッションでも校正・点検・除外の原本名が step19b に届く。ノートの「出力」タブに、セッションの値（校正原本・LAeq・点検原本・点検方式）を埋めた**帰宅後のコマンド**を出す | T48（校正 ZOOM0001・点検 ZOOM0002・事象 ZOOM0003・除外 ZOOM0004 を同じ raw/ に置き、追加引数なしで 変換→校正→切り出し が rc=0、切り出しは ZOOM0003 のみ）。ノートの実出力を流す `scripts/_note_pipeline_check.py`（3 セッション・原本一覧と切り出しが一致・step19c エラー 0） |
| **H02** 取り消しで原本番号がずれる | 「取り消し」を 3 択に分けた: **録音していない（番号そのまま）／録音した→負例として残す（番号を進める）／録音した→除外して次へ（番号を進め、除外一覧と備考「除外: …」に残す）**。待機画面に「録音機の次の番号」欄を置き、録音機の表示と違えばその場で直せる。除外原本は step19d が備考から拾い（`excluded_files`）、step19b が「除外（記録紙の備考の指示）」と表示して飛ばす | jsdom 試験 H02 ×3（除外→次は ZOOM0004、録音していない→番号据え置き、番号 7 に修正）。T48 の除外 ZOOM0004 |
| **H03** 別クラスを挟むと同じ群にできない | 重なりの候補を「直前の 1 行」から「**同じテイクの同じクラスの全事象**」に変えた。候補ごとに「事象 k（ラップ）と重なった」を出し、既存の群があれば合流、無ければ新しい G を両方に付ける。単独に戻すと 1 台だけになった群は自動で解消。事象の削除・クラス変更でも群の台数を照合（`fixGroups`）。n_car は出力時に群の台数から計算 | jsdom 試験 H03（車A→クラクション→車B で車B を車A と G1 に。events.csv: 車 2 行が n_car=2/G1、クラクションは n_car=1/群なし） |
| **H04** 未終了・空テイクが黙って抜ける | 「出力」タブの先頭に**未処理一覧**: 閉じていないテイク（事象 n 件が CSV に無い）、事象 0 件のテイク。片づくまで**コピー・共有ボタンを止める**。空テイクはその場で「負例にする／除外する」を選べる。最後の事象を消すと案内が出る | jsdom 試験 H04 ×2（閉じていないテイクでコピー不可、唯一の事象を消す→「事象 0 件」→負例にして解除） |
| **H05** ラップ・歩行ペアの検査不足 | ラップは**分・秒の 2 欄**（ストップウォッチ 01:23.4 → 分 1・秒 23.4 = 83.4 s）。10 分超・前の事象より小さい値は入力時に注意、閉じるときは「10 分超」「ラップ空」「象限空」「**横距離空**（距離クラス）」「歩行対比で pair 無し」を列挙して止める（2 度押しで強行可）。歩行対比セッションでは pair の既定を前回の値にし、静止・歩行がそろっていない pair を出力タブで注意。step19d は 区分=歩行対比 を **pair の有無と無関係に 歩行 に正規化**し、pair 欠落を警告 | jsdom 試験 H05 ×2、歩行対比 ×3（pair 無しで閉じられない、静止だけの注意→歩行を足すと消える、横距離空で止まる）。T49（区分の正規化と警告） |
| **H06** 前 1 打に 4 打用コマンド | 点検方式をセッションに持ち（`点検方式` 列 = `4方位`／`前1打`）、コマンドは前 1 打なら `--expect 前` を付ける。**4 方位の点検を記録済み**（`check4Done`）になるまで前 1 打は選べず、初回は 4 方位が既定。記入用 CSV・README にも `点検方式` 列を追加 | jsdom 試験 H06 ×3（初回 4 方位固定、記録後は前 1 打が既定で `--expect 前`、session.csv に 点検方式） |

## 変わった様式

- session.csv: `点検方式` 列を追加（`校正原本`・`点検原本` の後）。`備考` に「除外: a.wav b.wav」と書くと切り出し対象から外れる
- 注釈（step19d 出力）: `calibration_id` = 校正原本の stem、`check_file`・`点検方式`・`excluded_files` 列を追加、`<out>_sessions.csv` を併産
- step19b: `check_file`・`excluded_files`・セッション表の原本を除外して未対応検査を行う（検査自体は無効化していない）

## 自動試験

- `python scripts/test_realsmoke_v2_units.py` … 61 項目（T48/T49 追加）
- `node scripts/test_helmet_note_jsdom.js md/design/実録_記入用CSV_2026-09-05/ヘルメット収録ノート.html` … 21 手順（要 `npm i jsdom`）。最後にノートの実出力 `note_session.csv` / `note_events.csv` を書く
- `python scripts/_note_pipeline_check.py <その2つがある場所>` … 実出力を 変換→校正→切り出し（event/negative）→検査 に流し、原本一覧と切り出しの一致を確認

## 未検証（本人）

スマホ実機での表示、コピー／共有、再起動後の保存、オフライン起動。配布形態は claude.ai の非公開ページ（同じ URL で更新）。
