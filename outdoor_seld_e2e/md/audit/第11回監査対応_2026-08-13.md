# 第11回監査（実体検査・不合格）対応記録（2026-08-13）

方針: 「文書の宣言」ではなく「実体」を直す。pptxは生XML（a:t抽出）で独立照合し、
実録コードは単体テストで挙動を証明する。

## 高重要度

| # | 指摘 | 対応 |
| --- | --- | --- |
| 高1 | PPTX実体・生成元に過大表現残存 | S3「首元の振動で伝える」→「将来的には…想定」／S7「ラベル誤差ゼロ」→「生成誤差ゼロの教師ラベル（シミュレータ内部基準・モデル化誤差は残る）」／S7「他に存在しない」→「調査した範囲で存在しない」／S8タイトルに「規則v3.4」明記／S10 40kHzに「仮定受信機・設計音圧」。生成スクリプトも同期。**生XML照合で旧文言10種=0件・新文言8種=全反映を確認**（deck_xml_verify） |
| 高2 | 合格が自己申告ベース | 本対応から照合方法を変更: python-pptxを経由しない**zipfile+正規表現のa:t抽出**で検証（第10回の対応報告方式は撤回）。第10回「合格」記録に無効の注記は第11回報告(md/audit/第11回監査_実体検査_2026-08-13.md)が正 |
| 高3 | 問答C1「聞き逃しがない」 | 「疲れや注意力に左右されない常時監視（この開発評価の範囲では遠方配置の未検知ゼロ。一般環境での保証ではない）」へ修正 |
| 高4 | step19が96kHz拒否 | 96000受理＋resample_poly(1,4)を実装。docstringに96kHz原本保持→24kHz出力の分岐を明記。**テストT1で96kHz→24kHz/長さ1/4を確認** |
| 高5 | 誤警告が先頭10秒のみ走査 | **step20_realsmoke_score_v2.py（新規・正本）**: クリップ実長で全区間走査（注釈窓とpred最大フレームの大きい方）。**テストT2: 120秒負例の50秒地点の発火を検出（旧実装なら0件）・露出120s計上** |
| 高6 | event_id無視・旧v1規則 | v2はclip×class×event_idの窓[t_start−1, t_cpa+1]へ発火を**貪欲割当（1発火最大1イベント）**。規則はv3.4（距離≤1.5×2フレーム連続＋方位連結±60°）を可変長で再実装（定数はstep12_notify_v33と同一）。警告音クラスは2連続確認＋不応期3s。**テストT3（割当）・T4（±60°連結の発火/非発火）で確認**。旧step20には非推奨ヘッダ |

## 中・低

| # | 指摘 | 対応 |
| --- | --- | --- |
| 中7 | 対応あり統計が未実装 | v2に実装: McNemar厳密検定（--pred-b対応あり比較）／リード差のpaired bootstrap 95%CI／誤警告率のPoisson厳密区間（常時出力）。**テストT5a-c: p(0,8)=2⁻⁷・0件/100分の片側95%上限1.797回/h・定差CI検算** |
| 中8 | 正本分裂 | 生成スクリプト冒頭の「HTMLと同内容」宣言を削除し正本宣言に変更。HTML・原稿・骨子・ClaudeDesignプロンプトの4ファイルに**【旧版・参照禁止】バナー**（正本=pptx/台本/問答集を明記） |
| 低9 | 作業ツリー | ロックファイル削除をコミット・.gitignoreに`**/~$*`追加・PSELDNets配下の__pycache__削除（サブモジュールポインタは従来どおり非コミット） |
| 補足 | S6/S10文字超過候補 | PNG目視で確認: S10問題なし。S6は「ドップラー」チップ折り返し＋「大気吸収」とマイク図の重なりを実確認→チップ幅62pt・座標修正（スクリプト＋pptx両方）。修正後PNGで解消確認 |

## テストログ（scripts/test_realsmoke_v2_units.py）

```
[PASS] T1 96kHz受理 (fs=24000, n=48000)
[PASS] T2 長尺負例で50s地点の発火を検出 (n_false=1, exp=120.0s; 旧実装なら0件)
[PASS] T3 1発火は1イベントのみ通知成功 ({'1': True, '2': False})
[PASS] T4 方位連結（40°=発火/70°=非発火） (in=[(11, 50.0)], out=[])
[PASS] T5a McNemar厳密 p(0,8)=2^-7 (p=0.007812)
[PASS] T5b Poisson片側95%上限（0件/100分）≈1.80回/h (hi=1.797)
[PASS] T5c paired bootstrap（定差1s）CI=[1,1] ((1.0, 1.0, 1.0))
ALL PASS (7 checks)
```

## 残る既知の限界（正直に）

- v2の警告音クラス規則（2連続確認＋不応期3s）はv1の踏襲であり、v3.4の距離トリガ
  対象は車・キック・バイクの3クラス（v12b系と同じ）。サイレン等に距離トリガは適用しない
- 8クラスのクラス番号対応（siren=0…bike=7）は実録予測CSV生成時に要照合
- 本人の編集用デッキ（iCloud夏ゼミ/2026版）は別ファイルであり本対応の対象外
  （修正対照表は2026-08-13 16時台のやり取りで提示済み）

## 再監査1巡目（不合格・2026-08-13 19:02）の残4点への対応（19:20追記）

| 指摘 | 対応 |
| --- | --- |
| 高1残: S3「3段階の振動で伝える」（現在形） | 「3段階で通知する（提示は首元振動を将来構想）」へ（スクリプト＋pptx実体、生XMLで旧0/新1確認） |
| 高5残: 負例窓の二重計上 | 注釈イベント窓[t_start−1,t_cpa+1]内の発火をマスクし、露出も重なり秒数を控除（テストT7: イベント成功＋誤警告0＋露出53/60s） |
| 高6残: 警告音規則の定数相違・中通知欠落 | v1の事前決定値（3フレーム連続・不応期5s・方向±45°）に修正（T6/T6b）。距離クラスはT2=3.0mまで走査しエピソード最小距離で強/中を判定、中到達をmid_reachとして記録（T8: d=2.0mは強にしない） |
| 中7残: 比較キーclip欠落・bootstrap粒度 | キーを(clip,trial,event_id,class)に修正。paired bootstrapをクリップ単位の復元抽出に変更（T5c）。テストは計11件 ALL PASS |
