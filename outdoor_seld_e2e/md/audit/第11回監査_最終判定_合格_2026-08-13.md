最終判定：合格（対象HEAD `b75e615ab`）

- 高1残：slide3生XMLで旧文言0件、新文言1件。生成元にも同一文言を確認。
- 高5残：イベント窓内発火の除外と、窓の和集合による露出控除を確認。T7は通知成功・誤警告0・露出53秒。
- 高6残：`3フレーム／50フレーム／±45°` がv1定数と一致。中通知は `≤3.0m` で判定。45/46°、3.0/3.01m境界も合格。
- 中7残：比較キーは `(clip, trial, event_id, class)`。bootstrapはクリップ復元抽出。
- リグレッション：T2〜T8の10チェック＋境界2チェックを既存venvでネイティブ実行し全件合格。T1は一時ファイルを生成するため再実行せず、対象差分で不変を確認。`git diff --check`異常なし、前後ハッシュ不変、作業ツリーはクリーン。

実録コード経路の使用条件：既存venvで96 kHz原本は `step19_realsmoke_convert.py` を通して24 kHz化し、採点には旧版ではなく `step20_realsmoke_score_v2.py` を使用すること。距離クラスを採点する予測CSVには距離列を含め、8クラス番号対応を事前照合し、注釈は `(clip_id, trial, event_id, class)` が一意となるよう作成する。方位連結±60°、警告音v1定数、イベント窓±1秒を事後変更せず、比較時は同一クリップ・同一注釈を用いること。
