# 実録プロトコルの敵対的レビュー（Astra・2026-09-05〜06・判定「不合格」）への対応計画 — 2026-09-06 02:40

元 = `md/audit/敵対的レビュー_実録プロトコル_2026-09-05_Astra.md`（R01〜R15、残条件 6 つ）。
判定は妥当と考える。指摘は「コードで直すもの」と「手順書・事前登録で決めるもの」に分かれるので、担当を分けて進める。
手順書（当日の手順書・記入用 CSV・ハンドブック）は 9/15 スライドのチャットが編集中のため、このチャットは**コード側**と**決めるべき事項の整理**を受け持つ。

## 1. 担当の分け方

| 指摘 | 種類 | 担当 | 対応 |
| --- | --- | --- | --- |
| R01 校正の引継ぎ | コード＋手順 | このチャット（コード）／手順書チャット（手順） | `step19` に校正ファイル指定（`--calib`）を足し、補正量と `calibration_id` を出す。`step19b/19c` が `calibration_id`・録音機ゲイン・原本名を必須列にする |
| **R02 採点器が v3.4** | コード | **このチャット** | `step20_realsmoke_score_v3.py` を新設: v4.3＋警告音 hold の正規実装（`step12_notify_v43` の `run_rule3`・`winner.json`）を呼ぶ。F（前方）は既定で除外し別集計。旧 v2 は「v3.4（比較用）」の名前で残す |
| **R03 象限比較の時点** | コード＋定義 | **このチャット** | ①型分類の窓（CPA−2.5〜−1.5 s・記述用）、②通知成功の窓（[開始−1 s, CPA＋1 s]）、③方向の比較時点（**注釈した象限と同じ窓の推定方位の中央値**＝発火時刻ではない）を分けて実装。分母を「全イベント」「通知できた例」の両方で出す |
| R04 現場 CSV → 解析入力 | コード＋記入様式 | このチャット（変換器）／手順書チャット（列の定義） | `step19d_field_csv_to_ann.py` を新設: `session_id × take_id` を一意キー、列名の対応表（象限→quadrant など）、原本名の保存。模擬データ（2 セッション・校正・正例・負例・複数イベント）で最後まで通す試験を `test_realsmoke_v2_units.py` に追加 |
| R05 CPA の正解 | 手順・予備試験 | 手順書チャット／本人 | 独立な観測（通過位置の目印＋同期した時刻記録）の方式と予備試験。音量ピークは補助 |
| R06 複数車を 1 行 | 記入様式 | 手順書チャット | 1 車両 1 行＋`overlap_group_id` |
| R07 事前登録の更新 | 文書 | 本人（9/15） | 変更票 1 枚（ヘルメット・収録方式・用途・除外規則）。調整用/最終評価用の分離は `実録_調整用と最終評価用の分離_2026-09-05.md` の案を採否 |
| R08 超音波 | 決定 | 本人 | 統制発信の事前試験を残すか、統制超音波評価を撤回するか |
| R09 校正の点検 | 手順 | 手順書チャット | 導入時は既知の複数方位で軸・符号を検証、再装着時の短い点検 |
| R10 CPA 前 8 秒 | コード＋手順 | このチャット（フラグ）／手順書チャット（録音開始の規則） | `step19b` で履歴不足（CPA が 8 s 未満）のクリップに `history_short=1` を付け、`step20 v3` は主要評価から外して件数を報告 |
| R11 ヘルメット＝自由音場 | 文書 | 手順書チャット | 主張を「ヘルメット構成下」に限定。横距離の基準点（車の経路からの距離）を図示 |
| R12 EV/HV | 記入様式 | 手順書チャット | 動力方式・車両区分・確認方法を分ける |
| R13 「学習に使えない」 | 文書 | 手順書チャット | 「今回は評価専用（設計判断）」に改める |
| R14 道路使用 | 文書 | 本人 | 断言を外し、実施形態ごとの判断根拠を残す |
| R15 風防 | 文書 | 手順書チャット | フォーム同梱・ファー別売を区別 |
| W8/D10 機会枠 | コード | このチャット | `step19c --plan` に機会枠（サイレン等）の「0 本でも可」を接続（`--opportunity D=8` のように上限目標と扱う） |

## 2. コード側の順番（このチャット）

1. **step20 v3**（R02・R03・R10 のフラグ・F の別集計・分母の併記）— 最優先。合成データの統一採点器（`_score_unified.py`）と同じ規則・同じ帰属の考え方に揃える
2. **step19 の校正ファイル経路**（R01）と `calibration_id` の伝播
3. **step19d 現場 CSV 変換器**（R04）と模擬データの通し試験
4. `step19c` の機会枠（W8）

各段で `scripts/test_realsmoke_v2_units.py` に試験を足し、合格試験は「エラーが消えた」ではなく「期待する発火・分母・時刻・校正量になった」ことを証跡にする（監査の注意）。

## 3. 本人に決めてもらうこと（9/15 まで）

- ~~R07~~ → **A′ に決定（本人 2026-09-06 03:45）: 事象テイク 210 本は全部評価専用。合成の調整は「負例露出 100 分＋校正・予備試験の録音＋210 本の外の統制試行（調整用と明記）」だけから行う。**
  `実録_調整用と最終評価用の分離_2026-09-05.md` を A′ に書き換え、変更票 `実録_事前登録の変更票_2026-09-06.md` を作成
- ~~R08: 超音波の統制評価を残すか撤回するか~~ → **撤回**（本人決定 2026-09-06 03:38。ハンドブック・機材リスト・手順書を訂正済み）
- ~~R05~~ → **方式確定（本人 2026-09-06 03:45）: スマホのラップをリアルタイムで押す。目印も予備試験も置かない。**「車の前端が肩の線を通った瞬間」に押す規則で、
  反応時間のあいだに車の中央が真横に来るので補正なし（残る誤差 ±0.3〜0.5 s ＜ 採点の許容 ±1 s）。正解の最接近時刻はラップであり、音量最大では決めない
- ~~R14~~ → **決定（本人 2026-09-06 03:42）: 警察への電話確認はしない。人の少ない早朝・夜の時間帯に 1 人・歩行・設置物なしの受動観測で行う。**
  実施形態（時間帯・人数・設置物なし・滞在時間）を記録紙に残す。報告では「早朝・夜の静かな条件」であることを条件として明記する（人通り・交通量が少ない＝重なりの少ない易しい条件）

## 4. 進捗

- 2026-09-06 02:40: 計画作成。step20 v3 に着手
- 2026-09-06 03:10: **step20 v3** を新設（`scripts/step20_realsmoke_score_v3.py`）。v4.3＋hold の正規実装を呼ぶ・成功窓 [開始−1, CPA＋1]・方向は注釈窓（CPA−2.5〜−1.5 s）の推定方位の中央で比較・
  前方 F／履歴不足（CPA<7.5 s）／多重車 n_car≥2 を主要評価から分離して別集計・方向の分母を 2 通り併記。模擬試験 `scripts/_mock_realsmoke_v3.py`（6 行）で発火・分離・方向を確認。
  残り: R01 校正ファイル経路、R04 現場 CSV 変換器と通し試験、W8 機会枠
- 2026-09-06 04:10: **コード側の残り（R01・R04・R10・W8）を実装**。step19 `--calib`（校正録音から補正量を 1 回だけ・`calibration_<id>.json`）、
  step19b に `calibration_id`/`gain_db`/`history_short` 列、step19c は (session_id, take_id) で一意・S9 校正 id 検査・`--opportunity`、
  step19d（現場 CSV → ann_orig の変換規則を 1 か所に）。合格試験 T38〜T41 を追加し **50 項目 ALL PASS**
  （旧経路が停止すること・新経路の補正量が json と一致し本番に適用されること・2 セッションの take 1 が衝突しないこと・機会枠が警告にならないこと・履歴不足のフラグ）。
  パイプラインの規則 = `md/design/実録_解析パイプライン_現場CSVから採点まで_2026-09-06.md`。手順書側（R05〜R09・R11〜R15）と本人の決定（§3）は未了
- 2026-09-06 03:50: 本人「これも直して」→ **手順書側の文言も訂正**（手順書・機材リスト・記入用 CSV README・ハンドブック）: R03 の根拠（「通知層の判定窓と一致」→「方向を比べる時点」）、
  R01（ゲイン変更時の再校正・帰宅後の経路を step19d→step19 --calib→step19b→step19c→step20 v3 に）、R04（結合ではなく変換）、R05（音量最大≠最接近の注意と独立観測の予備試験）、
  R11（横距離の基準点＝車の通過経路・自由音場を主張しない）、R13（「使えない」→設計判断）、R14（装着＝一律不要と断言しない）、R15（フォーム同梱・ファー別売）。
  未了（本人の決定）: R05 の観測方式、R06 1 車両 1 行の様式、R07 用途の確定、R08 超音波、R09 校正点検の手順、R12 EV/HV の確認方法列
