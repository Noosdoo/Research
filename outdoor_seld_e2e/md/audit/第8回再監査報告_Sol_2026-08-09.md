# 第8回再監査報告（外部監査人 Sol）

## 最終判定: **不合格**

保存された表の加算・除算は整合し、捏造の証拠はない。しかし、主要指標を生む距離トリガの意味論、テスト網、v12の分母、未使用fold表記に重大な欠陥がある。「条件を全消化済み」「発表用確定値」とすることは認められない。

---

## A層: 原監査依頼

[重大度 高] 「推定距離≤閾値が2フレーム連続」は、同一車両の連続ではない。前フレームは単なる真偽値で、現フレームの最小距離候補との方位・トラック継続を確認しないため、車A→車Bでも車Bを2連続として強通知にできる。70.0%の367台中366台がこの経路なので中心指標に直結する新規見落とし — 根拠(`scripts/step12_notify_v3.py:86-95`; `md/audit/第8回監査報告_Claude代行_2026-08-06.md:26,83`) — 再現手順: 予測CSVをclip/frame順に走査し、各hitについて現フレーム採用方位から±25°以内の候補が前フレームにもあるか確認する。存在しないhitを除いて混同行列を再計算する。

[重大度 中] 「活動マスク付き距離MSE」という仕様と実装が一致しない。活動マスクは正解距離を0にするだけで、損失自体は全クラス・全トラックの距離出力に対して計算される。このため非活動距離出力も0へ回帰させ、共有表現へ勾配を流す — 根拠(`md/audit/第8回監査依頼_SDE役割③_2026-08-03.md:25`; `server_sde/sde_patch.py:217-243`) — 再現手順: 全活動値0のtargetと、距離chだけ非0のoutputを`Losses_SDE`へ与える。真の活動mask損失なら`loss_other=0`だが、現実装では正になる。

[重大度 中] T6は、代行報告が要求した実行時`_getitem_sde`の検証ではない。CSVから再構築したH5を先頭200本で比較するだけで、セグメント境界、`i0/i1`、padding、datasetパス計算を含む距離注入経路は未試験のまま — 根拠(`md/audit/第8回監査報告_Claude代行_2026-08-06.md:66-71`; `server_sde/sde_patch.py:108-134`; `server_sde/test_sde_units2.py:129-152`) — 再現手順: 実際の`DatasetMultiACCDOA.__getitem__`を先頭・中間・末尾セグメントで呼び、返却された第5軸を対応するH5 sliceと比較する。空ラベル、短い末尾、paddingを別ケースにする。

[重大度 低] 距離採点は方位ゲートなしのgreedy最近傍で、同じframe/classなら方位が大きく外れた誤検出でも必ずペアにする。「ペア成立91.3%」は方向的に正しい対応率ではない。実害量は要確認 — 根拠(`scripts/_score_sde_dist.py:79-85`; `out/step12_notify_v11sde/dist_score.md:3`) — 再現手順: 各成立ペアの`circ_diff`を保存し、15°・25°・90°超を数える。25°ゲート付きMAE・ペア率と比較する。

[重大度 中] 代行報告済みの出典層問題は概ね修正されたが、別文書に「全音源で規格帯較正済みで不可侵」と、警笛の省令値未確認が併存する。規則値/設計値の混同は文書横断では未解消 — 根拠(`md/design/v12設計書_2026-08-05.md:169,175`; `md/seminar/ゼミ発表骨子_20260804.md:87,94-96`) — 再現手順: 「規格帯」「法規」「省令」を全mdで検索し、一次資料未確認のサイレン・警笛・踏切を設計値へ統一する。

保存表の算術については、149,335ペアがクラス別件数の和、107,379車ペアが距離帯別件数の和と一致し、367/524、515/525等の百分率も再計算一致した。ただし、これは上記の採点定義が妥当であることを意味しない。

---

## B層: Claude代行監査の監査

[重大度 高] 代行監査は、主要強通知のほぼ全部を生む独立トリガについて「同一物体の2連続」を確認しなかった。fold3追試も同じ欠陥ルールを再現しただけであり、意図されたルールの汎化確認にはなっていない — 根拠(`scripts/step12_notify_v3.py:90-94`; `md/audit/第8回監査報告_Claude代行_2026-08-06.md:26,83,188-190`) — 再現手順: A層の方位連続版でval/fold3を再採点し、69.5%・76.8%との差を報告する。

[重大度 中] 代行報告は自ら「T6=getitem照合」を条件にしたのに、追記では別内容のH5照合をもって消化扱いした。自己監査による合格条件のすり替え — 根拠(`md/audit/第8回監査報告_Claude代行_2026-08-06.md:66-71,196-199`; `server_sde/test_sde_units2.py:129-152`) — 再現手順: 原条件と実テストの到達コードを比較し、`_getitem_sde`が一度も呼ばれないことを確認する。

[重大度 中] 「全数値を独立再実装で再現」の生証拠が保存されていない。報告自身が検算スクリプトは揮発したと明記し、T4-T6/job317の標準出力ログもリポジトリにない。表の内部整合は確認できるが、独立再現の主張そのものは外部再監査不能 — 根拠(`md/audit/第8回監査報告_Claude代行_2026-08-06.md:181`; `PROGRESS.md:2125-2129`) — 再現手順: `git log --all -- '*w0*' '*317*'`とファイル検索を行い、起動sbatch以外のjob317ログ・検算成果物がないことを確認する。

[重大度 中] 「fold3未汚染」は過大表現。v11 SDE予測が未生成だったことは狭義には正しいが、同じfold3をv12初回推論で閲覧した事実が設計書にある。規則はそれ以前から実質固定されていたため直ちにチューニングを示すものではないが、「未使用fold」とは呼べない — 根拠(`md/audit/第8回監査報告_Claude代行_2026-08-06.md:167`; `md/design/v12設計書_2026-08-05.md:198-202`) — 再現手順: `git log --oneline -- scripts/step12_notify_v3.py`で規則変更時点を確認し、設計書のfold3閲覧記録と時系列化する。

[重大度 低] 10m張り付き率が「54.9%」と「60.7%」で併存する。前者は全予測行、後者は成立ペア内と推定できるが、分母が明記されていない — 根拠(`md/audit/第8回監査報告_Claude代行_2026-08-06.md:28,191`; `out/step12_notify_v11sde_v2/dist_score.md:5`) — 再現手順: 全予測行と成立ペアの両方で10.00m率を計算し、分母付きで2値を併記する。

したがって、2026-08-06 20:00時点の「条件付き合格」は妥当だったが、追記の「条件1–5すべて消化」「過学習懸念解消」は妥当ではない。

---

## C層: 監査条件の消化

### C1 fold3凍結追試

[重大度 低] ローカルgitの順序は確認できた。`5fc1df6cf`（21:24:22）→sbatch追加`54f88f1af`（21:30:42）→結果`b3b9cf085`（21:36:24）の順である。ただし実ジョブは実行時SHAを出力せず、サーバ作業木が5fc1だったことまでは証明しない — 根拠(`server_sde/v11_fold3_confirm.sbatch:7-11,30-43`) — 再現手順: `git log -n 15 --format=fuller`で順序を確認する。再実行時は冒頭で`git rev-parse HEAD`と`git diff --exit-code`をログへ保存する。

### C2 W_DIST=0対照

[重大度 中] sbatch上はW_DIST以外を揃える意図を確認できるが、run3用sbatchは実行後に再構成された記録で、W0の0.072もPROGRESS/代行報告だけに存在する。「劣化なし」ではなく「単一paired seedのfold2で劣化を観測せず」が妥当 — 根拠(`server_sde/train_sde_run3.sbatch:8-11,15`; `server_sde/train_sde_w0.sbatch:8-10,20`; `PROGRESS.md:2125-2128`) — 再現手順: run3/job317の生ログ、seed、best epoch、checkpoint名を回収し、同epoch比較とbest比較を分ける。少なくとも別seedを追加する。

### C3 T4-T6

[重大度 高] T4/T5のコードは不足経路を部分的に覆うが、T6はgetitem条件を満たさず、実行ログもない。よって条件消化とは認めない — 根拠(`server_sde/test_sde_units2.py:129-154`; `md/audit/第8回監査報告_Claude代行_2026-08-06.md:71,199`) — 再現手順: 実Datasetを通すT6を追加し、T1-T6の標準出力・環境・commit SHAを成果物として保存する。

### C4 v3.2再生成

[重大度 高] no-opは「件数を表示する」ところまで修正されたが、帰属不能トリガは主要な誤強・抑制率へ反映されない。valで強40/中135件、fold3で強20/中70件が「誤報か実車か未判定」のままなので、誤報の計上穴は閉じていない — 根拠(`scripts/step12_notify_v3.py:162-166,222-224`; `out/step12_notify_v11sde_v2/notify_v3_val.md:19`; `out/step12_notify_v11sde_fold3_run3/notify_v3_val.md:19`) — 再現手順: 連続フレームを1警報episodeにまとめ、GT存在、方位スイープ、完全誤報へ分類し、強誤報/clipまたは時間率へ組み込む。

### C5 run1 fold3距離の不採用

[重大度 中] 現在の`fold3_run1.csv`をそのまま採用しない判断は正しい。しかし「復号不能」は誤り。旧run1は全chにtanh、HEADはxyzだけtanhなので、履歴コードまたは明示変換で復号可能 — 根拠(`server_sde/sde_patch.py:153-174`; `server_sde/v11_fold3_confirm.sbatch:30-33`; `PROGRESS.md:2107-2108`; git commit `d2ed6e1a2`) — 再現手順: `git log -p -n 1 d2ed6e1a2 -- server_sde/sde_patch.py`で旧活性を確認し、旧コードでep094を再推論する。既存CSVからの近似なら正距離は正の行について`10*tanh(d_HEAD/10)`で復元できる。

### C6 v12 w3切替

[重大度 高] v12採点・新クラス通知は予測CSVに1行あるクリップだけを巡回し、完全無検出クリップを分母から落とす。正規valは1,800本だが、バイク表はGTトラック225台＋「バイクなし1,409本」なので、巡回クリップは最大1,634本、少なくとも166本が欠落する — 根拠(`scripts/_v12_score.py:40,67`; `scripts/step12_notify_v12.py:72,89`; `md/design/v12設計書_2026-08-05.md:202`; `out/v12_notify_newcls_w3/notify_newcls_val.md:30-35`) — 再現手順: prediction keysではなく1,800件のmetadata manifestを巡回し、予測なしを空辞書として全指標を再計算する。

[重大度 中] v12距離表は実際には8クラス・1,800本系なのにタイトルが「val 1,200本」、クラス別表示は6クラスだけで、全体MAEには未表示の新クラスが混入する。6クラス表示のn合計174,495に対し総ペア201,752で、27,257ペアが表から消える — 根拠(`scripts/_v12_score.py:34-35,120`; `scripts/_score_sde_dist.py:92,103`; `out/v12_score_w3/dist_score.md:1,3`) — 再現手順: `range(8)`と8クラス名で再出力し、core6・new2・全8のMAEを別表にする。

[重大度 高] w3はG4失敗を見た後に同じfold2 valへ合わせた開発モデルで、G5は0.087のまま不合格、kick苦手SEDも−5.4pt。それにもかかわらず発表骨子は8クラスについて「未使用foldで汎化確認」「過学習を排除済み」とする。実際のfold3確認はv11系で、v12 fold3は既に閲覧済み — 根拠(`md/design/v12設計書_2026-08-05.md:198-202,222-235`; `md/seminar/中間発表骨子_2026-08-07.md:89,98`; `server_sde/v12_g45_w3.sbatch:7-10`) — 再現手順: 発表骨子の未使用fold記述を削除し、w3を「fold2開発値・G5未達」と表示する。確定には新しい未使用評価集合を事前固定して一回だけ採点する。

---

## 再合格に必要な最小条件

1. 同一物体の方位連続を要求する距離トリガでval/fold3/v12を再採点する。
2. 全scorerをmetadata/manifest基準の全クリップ巡回へ修正する。
3. 実`DatasetMultiACCDOA.__getitem__`を通すT6を追加し、T1-T6とW0の生ログを保存する。
4. 帰属不能トリガをepisode単位で監査し、誤報KPIへ反映する。
5. run1を旧活性で復号してfold3比較を回収する。
6. v12 w3を開発値と明記し、「未使用foldで汎化確認」を撤回する。距離表も8クラス・1,800本表記へ直す。

リポジトリへの変更・作成・削除は行っていない。

Codex session ID: 019fe395-d985-76f3-b448-897a7797c8ce
Resume in Codex: codex resume 019fe395-d985-76f3-b448-897a7797c8ce
