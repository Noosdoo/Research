# セルフ点検 — 実録の文書と道具の整合（2026-09-07 06:40）

次の外部監査（Astra）の前に、自分で敵対的に見直した。**対象**: 実録の手順書・記入用 CSV・ハンドブック・機材リスト・解析パイプライン、
変換器 step19d・切り出し step19b・検査 step19c・採点 step20 v3・統一採点器・ノート（収録記録シート）。
方法: (1) 失効した用語の横断検索（声メモ・動画・超音波・チェスト・三脚・手拍子 1 打・`<session>_calib`・1 行にまとめて・相殺）、(2) 各道具の試験の網羅の確認、(3) 前回監査の残条件の再照合。

## 1. 見つけて直したもの（文書の古い記述）

| 場所 | 古い記述 | 直した内容 |
| --- | --- | --- |
| 手順書 §帰宅後 | `--calib raw/<session>_calib.wav`・`calibration_<session>_calib.json` | 校正原本（例 ZOOM0001.wav）・校正 id = stem。ノートが値入りで出す |
| 解析パイプライン §2 | `<session>_calib.wav` | 記録紙の `校正原本` |
| ハンドブック §機材・§儀式 | チェストハーネス／小型三脚、自転車ベル（校正音）、手拍子で時刻同期・声でスレート・ベル 4 方位・チェスト装着の校正 | ヘルメット、校正 60 秒は別ファイル・無言、点検は指パッチン 4 方位（合格後は前 1 打）、マイク高さの実測。旧手順は失効表示 |
| ハンドブック §注釈 | 正面で手拍子 1 打、n_car は 1 行にまとめて | 点検テイク＋step19e、1 車両 1 行＋overlap_group_id（R06 案・本人確認待ち） |
| ハンドブック §9 | `raw/<session>_calib.wav` | 校正原本 |
| 機材リスト | 方位校正は正面で手拍子 1 打 | 指パッチン 4 方位／前 1 打（step19e） |
| 記録紙テンプレート（紙版・不使用） | 手拍子 1 打 | 指パッチン 4 方位（注記のみ） |
| 収録前ゲート消化 | チェストハーネス購入 | 不要（取り消し線） |

残した古い記述（履歴として正しい・現行の手順ではない）: 実録スモーク計画書（7 月）、再設計の論点（8/22）、収録計画 v2 追補（7/28）、再設計レビュー（8/14）。
これらは「当時の計画」で、冒頭に現行への参照があるので触らない。

## 2. 道具側で確認したこと（試験で固定済み）

- 変換 step19d: 校正 id = 校正原本 stem（T48）、点検原本・除外・点検方式の引継ぎ（T48）、歩行対比の正規化（T49）、用途の保留（T54）、横距離の幅（T56）、clip_id = session__原本（T52）
- 切り出し step19b: 96 kHz＋校正 json（T50）、単一ファイル／--only の照合範囲（T50）、空 --calib-dir・CLI id 食い違い・区間の不一致で停止（T51）、同名原本の --session（T52）
- 採点 step20 v3: 歩行対 (pair, class)（T53）、警告音の方向 = 発火時刻（T55）、境界例の分離（T57）
- 統一採点器: 8 項目（test_score_unified_units.py）
- ノート: jsdom 22 手順（H01〜H06・R11・バックアップ）＋ 実出力を 変換→校正→切り出し（event/negative）→検査 に流して原本一覧と一致（_note_pipeline_check.py）

## 3. まだ本人の判断が要るもの（監査に「未確定」と書かれる項目）

1. 変更票 14〜16（複数車 1 車両 1 行・EV/HV の 3 列・4 方位点検）: 案を実装済み。**確認の一言で確定**
2. 距離の補正表（束 §6.1 判断 1）: 手順だけ登録済み。v2 モデルで作るかは 11 月
3. D2 雨レベル・D4 歩行比率・D7 風: 10 月の実測で埋める（v2final_params.json）

## 4. 自分で見つけた、まだ直していない弱点（正直に）

- **ノートのサーバ保存は全記録を 1 文書に写す**（上限 256 KiB）。210 テイク・20 セッション程度なら 100 KB 前後で収まる見込みだが、超えると写せなくなる。超えたら「セッションごとの文書」だけにする改修が要る（今は両方書いている）
- **ノートの取り消し 3 択は本人の操作に依存**する。録音機の実ファイル一覧と CSV の原本一覧の照合は帰宅後の step19b（未対応検査）でしか分からない。現場で気づく仕組みはない
- **横距離の幅の採点**は「境界をまたぐ幅は区分別の率に入れない」規則で、幅が広い記入が増えると主要評価の母数が減る。どのくらい減ったかを報告に必ず書く（step20 v3 は件数を出す）
- 96 kHz 原本の全体処理は合成音（12 秒）で試験した。**実機の 20〜30 秒・96 kHz・4ch** での所要時間は未計測（負例 30 分の分割は step19b がストリーム読みなので載るはず）
- 手順書・README・ハンドブック・ノートの 4 か所に同じ規則が書かれている。**正は解析パイプライン文書と変換器のコード**と決めてあるが、次の改訂で 4 か所を同時に直す必要がある（今回もそれで 6 ファイル）

## 5. 次の外部監査に渡す前の確認リスト

- [x] 失効用語の横断検索（上記）
- [x] 試験 70＋8 項目・jsdom 22 手順・通し 1 本が全部合格
- [x] 事前登録 v2 の下書き（`確定評価v2_事前登録_下書き_2026-09-07.md`）
- [ ] 本人: 変更票 14〜16 の確認、ノートのスマホ実機での試用（サーバ ✓ の表示・ファイル保存）
