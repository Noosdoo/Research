# 実録プロトコル 再監査2（Q01〜Q06・2026-09-06）への対応 — 2026-09-07 01:10

対象: [再監査2](実録プロトコル_再監査2_2026-09-06_Astra.md)。判定「不合格」の残条件 4 つに沿って、切り出し器（step19b）・変換器（step19d）・採点器（step20 v3）・配布文書を直し、
監査の反例をそのまま自動試験（T50〜T55）にした。試験は `python scripts/test_realsmoke_v2_units.py`（67 項目 ALL PASS）。

## 指摘ごとの対応

| 指摘 | 直したこと | 試験 |
| --- | --- | --- |
| **Q01** 96 kHz 原本と負例の個別処理で止まる | (1) `--calib-dir` の有効な校正 json があれば、96 kHz 原本でも `--gain-db` を要求しない（回転角 3 つは引き続き明示必須）。(2) `--in <1 ファイル>` または `--only a.wav,b.wav` のときは**その呼出しの対象だけ**を照合し（注釈があるのに音声が無い＝対象外の原本は数えない）、統合注釈全体の完全性は step19c `--cut` で見る。全件の不一致を無条件に許す変更はしていない | **T50**: 96 kHz・2 セッション（校正が 20 dB 違う）・正例 2・負例 2 本を 1 本ずつ `--append`・追加引数なしで rc=0、負例 4 クリップ、出力 24 kHz、各原本に自分のセッションの補正量、step19c rc=0 |
| **Q02** 校正 json の欠落・再校正・区間 | (1) `--calib-dir` が無い／空なら**停止**（0 dB で続行しない）。(2) `--calibration-id` を渡したときは注釈の id と食い違えば停止（以前は空文字に置き換えて無効化していた）。(3) json に記録した窓（`laeq_window_s`）と記録紙の `暗騒音区間_秒` を照合し、0.5 s 超違えば停止。(4) 再校正は**新しいセッション**（session_id を増やして校正からやり直す）に統一し、手順書・変更票 22 に明記。校正 id は校正原本の stem（H01） | **T51**: 空フォルダ・無いフォルダ・CLI id の食い違い・区間の不一致（記録紙 10-70 vs json 0-11）がすべて rc≠0 |
| **Q03** 別セッションの同名原本が上書き | (1) 変換器の clip_id を **`<session_id>__<原本 stem>`** にし、切り出しの出力名（`…_eN.flac`／`…_sNNN.flac`）と追記キーもそれを使う。原本名は `orig_file` に残る。(2) 同じ原本名が複数セッションの注釈にあるときは `--session <id>` を付けて 1 セッションずつ切り出す（付けないと停止）。(3) `--append` は既存の別セッションの行と clip_id が衝突すれば停止 | **T52**: rawA/ZOOM0001.wav（S1）と rawB/ZOOM0001.wav（S2）。`--session` なしは rc=1、S1 → S2 `--append` で 2 行と 2 音声が両方残り、校正もそれぞれ |
| **Q04** 歩行対が最後の 1 件に置き換わる | `walk_pairs_summary` を **(pair_id, class)** 単位に変更。同じ (pair, 状態, class) に 2 件以上ある対は「対象が 1 件に決まらない」として未集計にし、件数を報告に出す（黙って置き換えない）。クラス別に対応差を出す | **T53**: P1 は 車 −0.5・クラクション +1.0 の 2 対、P2（静止に車 2 件）は未集計として返る |
| **Q05** 用途の欠落・未知値 | 用途は `最終評価`／`調整用` だけを受け付け、空欄・未知値（「調整」など）は**保留**（`<out>_unresolved.csv`・警告・rc=1）にして評価用にも調整用にも入れない。配布 session.csv・記入例・ノートには用途列あり（ノートは選択式）。分離文書冒頭の「負例露出 100 分から調整」を「調整用の負例（別セッション）」に統一 | **T54**: 空欄・「調整」の 2 セッションが保留、最終評価だけ本体に |
| **Q06** 警告音の方向の時点不一致 | 方向の比較を「鳴り始めから 1 s の平均」から **通知が出た瞬間（発火フレーム）の推定方位** に変更。記録紙の「鳴り始めの向き」（瞬間の向き）と、その瞬間に近い 1 点の推定を比べる。未発火の場合だけ 1 s の中央を参考値に残す。変更票 18 を更新 | **T55**: −110°（右）から 1 s で −170° へ動く音。1 s 平均は −155°（後）で不一致になるが、発火時刻の推定は −110°（右）で一致 |
| **N07 文書** | 対応計画の「相殺・±0.3〜0.5 s」を失効表示、ハンドブックの「1〜2 秒のズレは害にならない（波形から読める）」を失効表示しラップ基準に統一、「最大 2 秒ほど」を「車長 4.5 m・1 m/s の構成例で 2 秒ほど（上限ではない）」に変更（手順書・変更票 2） | — |

## 残条件（監査の 4 項目）との対応

1. **96 kHz で処理全体を通す** → T50（2 セッション・校正変更＝別セッション・正例・負例 2 本・統合注釈・原本数 6／テイク 4／クリップ 6／負例露出 22 s を確認）
2. **原本・校正・用途の対応を失わない** → T52（同名原本）、T51（校正欠落・区間）、T54（用途）。再校正は「新セッション」に統一したので id の伝播問題は生じない（変更票 22）
3. **歩行対の対象と警告音方向の時点** → T53、T55
4. **配布版を一つの現行手順に揃える** → 手順書（校正は別ファイル全長 `0-60`、途中区間なら `--laeq-window` と一致させる、ゲイン変更 = 新セッション、ラップ基準）、変更票 2・18・22〜26、README の記入例。R06・R09・R12（変更票 14〜16）は案を実装済みで**本人確認待ち**のまま（監査の指摘どおり決定済みとは扱わない）。横距離の幅の記録（R11）は未着手

## 様式・出力の変更点（まとめ）

- 注釈 `clip_id` = `<session_id>__<原本 stem>`（以前は原本 stem）。既存の出力・採点入力とは名前が変わる
- step19d: `用途` が未確定の行は `<out>_unresolved.csv`。`<out>_sessions.csv` を併産（校正・点検・除外の原本名）
- step19b: `--only`、`--session` を追加。`--calib-dir` 必須条件を強化。記録紙の `暗騒音区間_秒` と json の窓を照合
- step20 v3: 歩行対の表は `pair/クラス`、未集計の対を明示。警告音の推定方位は発火時刻の値

## 未検証

スマホ実機での記録（ヘルメット収録ノート）は本人未検証。実録の音声はまだ無いので、本番原本での通し実行は 10 月の初回収録後。
